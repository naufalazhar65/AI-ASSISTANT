# AGENTS.md

## Project status

Project evolved (2026-09-03) from a **voice-first public app** to a **Personal AI Assistant (Mia)** — a multi-platform, Mia-style personal assistant. Voice/web foundation from v1.0 is retained and becomes the core; the new target is connecting to **Telegram & Discord** as channels around the same core. See `PRD_Real-Time_Voice_AI_Assistant.md` (v2.0) + `ROADMAP.md`.

Phase status: Fase 0–2 DONE (foundation + core assistant + Telegram/Discord channels). Fase 3/4 personal capabilities DONE (tools, scheduling, memory, monitoring, codebase QA, weekly insight, Mac control, Vision, habits/wind-down, email inbox, learn-from-correction). Fase 5 stability/security mostly DONE (auth, audit, rate-limit, config, backup, observability). Remaining gaps: WhatsApp/Email send, Google Maps Places (parked — needs API key), calendar/email/smart-home deeper integrations (optional). Live test loop: implement → gates (`npm run typecheck`, `npm run lint`, `npx tsx apps/web/verify.ts`, `npm test`) → live-verify → report → commit only after user approval.

## What this is

Personal AI assistant "Mia" — a command center for personal tasks, reachable from multiple channels (web text+voice, Telegram bot, Discord bot, more later) sharing ONE memory, ONE persona, and ONE tool set. Both a real-time voice assistant (from v1.0) AND a multi-platform chat/automation assistant. Inspired by Mia + Siri/ChatGPT Voice.

## Read the PRD & ROADMAP first

- `PRD_Real-Time_Voice_AI_Assistant.md` (v2.0) — source of truth: requirements, architecture (AI Provider + Channel Adapter abstraction), conversation model, state machine, security/test model, MVP scope, user stories, acceptance criteria.
- `ROADMAP.md` — phase-by-phase development guide (Fase 0 foundation → Fase 1 Core → Fase 2 Channels (Telegram/Discord) → Fase 3 Personal capabilities → Fase 4 Advanced → Fase 5 Stability/Security). Update checkboxes as features land.

Architectural principle (new in v2.0): **Channel Adapter abstraction** — each platform (web/telegram/discord) is an adapter that forwards text/voice to the SAME core Conversation Manager; the core never knows the platform. Adding a channel = adding an adapter, no core change.

Discord gotcha (2026-09-03): a first-ever **DM** arrives as a bare packet that discord.js can't map to a full `Message`, so `messageCreate` never fires even though the gateway receives raw `MESSAGE_CREATE` — symptom: **DM doesn't respond while guild chats work**. Fix: client must set `partials:[Partials.Channel, Partials.Message]` and the handler must `await msg.fetch()` when `msg.partial`. Both are present in `apps/web/src/channels/discord.ts.` Keep them when refactoring.

**Batch-selective tool confirmation (2026-09-15):** a turn that proposes several risky tools used to confirm only `needsConfirmation[0]`, mark the rest `Deferred` (`agent.ts`), and force the model to re-propose them one per turn — the cause of the slow one-command-at-a-time Discord/Telegram hunting loop. Now the agent accepts `confirm_calls?: {call,allow}[]` (singular `confirm_call` still supported for the web UI) and executes/declines every entry in one continuation; channels queue ALL pending calls and show one prompt: reply `ya` (all), `ya 1,3` (subset), or `tidak` (none). Shared parse/format lives in `apps/web/src/channels/replyChunk.ts` (`parseConfirmReply`/`pendingConfirmPrompt`; `bold` param = `**` Discord, `*` Telegram) so the two adapters can't drift. An unparsed reply re-prompts (never silently approves/drops). Unselected calls get a `Not selected` tool result (kept `Deferred` semantics → strict gateways never 400 on missing tool results).

## Repo layout

- `apps/web` — Next.js app (UI, hooks, audio, ConversationManager, provider, `/api/*` proxy routes). `src/audio`, `src/ai`, `src/components`, `src/hooks`, `src/app`, `src/lib`, `persona/`.
- `packages/state-machine` — explicit conversation state machine (invariant: invalid transitions impossible).
- `packages/ai-provider` — `AIProvider` abstraction + event types.
- `packages/mock-provider` — deterministic no-network provider for QA.

## Commands (npm workspaces at repo root)

- Install: `npm install`
- Dev web: `npm run dev -w @voice/web` → http://localhost:3000
- Typecheck: `npm run typecheck` (all workspaces)
- Lint: `npm run lint`
- Build: `npm run build`
- Verify core logic (no test framework yet; tsx scripts):
  - `npx tsx packages/state-machine/verify.ts`
  - `npx tsx apps/web/verify.ts`

**HABIT (wajib):** setelah selesai mengerjakan fitur/perubahan apa pun, ALWAYS restart server sebelum report. Kill **per port** dan TUNGGU port bebas (jangan `pkill -f "next dev"` saja + `sleep 3`): `lsof -ti tcp:3000 | xargs -r kill; pkill -f "next dev"; for i in $(seq 1 20); do lsof -ti tcp:3000 >/dev/null 2>&1 || break; sleep 1; done; rm -rf apps/web/.next; nohup npm run dev -w @voice/web > /tmp/mia-dev.log 2>&1 &` lalu cek `curl -s -m 10 http://localhost:3000/api/health` (harus `{"ok":true}`). **Kenapa:** prosedur lama sempat meninggalkan proses lama hidup berdampingan → DUA instance memproses pesan yang sama (owner dapat 2 prompt `remind_me`) dan Telegram mencatat `409 Conflict ... other getUpdates`. Setelah start, verifikasi `grep -c "logged in as" /tmp/mia-dev.log` = 1 dan tidak ada 409.
**Catatan (2026-09-17):** saat server dijalankan dari shell non-interaktif/otomatis (bukan Terminal biasa), `nohup next dev` **mati sendiri tanpa error** begitu shell pemanggil selesai — Next menutup diri saat stdin EOF (log berakhir di `[?25h`). Jalankan lewat tmux agar tetap hidup: `tmux kill-session -t mia 2>/dev/null; tmux new-session -d -s mia "npm run dev -w @voice/web 2>&1 | tee /tmp/mia-dev.log"` (verifikasi health di panggilan terpisah).

## Provider

- Registered in `apps/web/src/lib/providers.ts` (`PROVIDER_SPECS` = public metadata; `resolveProvider` = server-only env mapping; **this module is client-imported for the Settings UI — never add node built-ins here**; server-only key sources live in `serverKeys.ts`).
- `opencodego` — **OpenCode Go subscription** ($10/mo, CURRENT DEFAULT BRAIN via `.env.local`: `DEFAULT_AI_PROVIDER`/`NEXT_PUBLIC_DEFAULT_AI_PROVIDER`/`TELEGRAM_PROVIDER`/`DISCORD_PROVIDER` = `opencodego`; voice STT/TTS stays Groq). OpenAI-compatible gateway `https://opencode.ai/zen/go/v1/chat/completions`. Key: env `OPENCODEGO_API_KEY` **or** the opencode CLI login (`~/.local/share/opencode/auth.json` slot `opencode-go`, read by `serverKeys.ts ensureOpenCodeGoKey()` — wired in agent.ts, summarize.ts, consolidate.ts). Default model `deepseek-v4.1-flash` (`.env.local` `OPENCODEGO_MODEL`; spec default `glm-5.2`), 25 chat/completions models, LIVE-verified 2026-09-16 (glm-5.2/5.3/flash/5.1, kimi-k3/k2.7-code/k2.6, deepseek-v4.1-flash/v4-pro/v4-flash/v4-flash-vision-exp/flash, minimax-m3/m2.5, qwen3.8-max/flash, qwen3.7-max/plus, qwen3.6-plus, longcat-2.0, mimo-v2.5/-pro, hy4-preview, hy3, omen-alpha). NOT usable via chat/completions (listed by /models but the gateway rejects them): `grok-4.6`/`grok-4.5`/`kimi-k2.5`/`glm-5`/`qwen3.5-plus`/`mimo-v2-pro`/`mimo-v2-omni`/`hy3-preview` ("Model is unavailable"/not oa-compat), `gpt-5.6-luna`/`union-alpha`/`minimax-m2.7` (500), `muse-spark-*` (403 DataPolicyError — needs data-sharing consent).. **Go requirements: every request needs a stable `x-opencode-session` header** (derived from the user key in `runAgent`) **and a client User-Agent** (`mia-assistant/1.0`). Models list ≠ all gateway models (GPT-5.6 Luna = `/responses`, MiniMax/Qwen Max = Anthropic `/messages` — both unsupported by the agent loop).
- `groq` — free tier, voice STT/TTS + optional text. `opencode` — local `opencode serve` native API. `9router` — `LLM_API_BASE/KEY/MODEL`. `openrouter` — `OPENROUTER_API_KEY`. `mock` — canned replies, zero network.
- **Tool serialization gotcha (2026-09-07):** Mia's tool definitions carry a custom `risk` field; strict OpenAI-compatible gateways (OpenCode Go) REJECT extra fields on `tools[]` (`Extra inputs are not permitted, field: 'tools[N].risk'`) — `runOneCompletionOnce` serializes standard fields only (`type/function.name/description/parameters`). Groq/9router happened to tolerate it before.
- `MAX_TOOL_ROUNDS = 5` (was 3) — code Q&A legitimately chains search→read→answer; round-budget exhaustion threw "too many tool rounds" (user-visible "Terjadi kendala").

## Persona & long-term memory

- Persona files in `apps/web/persona` (`IDENTITY.md`, `DREAMS.md`, `SOUL.md`, `USER.md`) are the **single source of truth** for assistant identity/style and stable user facts. They're loaded server-side (`src/lib/persona.ts`, `loadPersonaPrompt`) and folded into the LLM system prompt every turn, so they persist across server restarts.
- **Live updates (Mia-style)**: the LLM may append a hidden `<persona>` tag to the end of a reply (`<persona>user.name=Naufal;soul.tone=formal</persona>`). `GroqStreamingProvider` strips the tag (never spoken) and writes each entry to the matching file (`user.*` → USER.md, `soul.*` → SOUL.md) via `POST /api/persona` (`src/lib/persona.ts` `upsertPersonaFact`).
- So a user introducing themselves ("my name is Naufal") or asking for a style change ("speak formally") is automatically persisted to `USER.md`/`SOUL.md` and remembered after restart.
- No per-user JSON memory store — the persona .md files are the only memory.

**Identity mapping (2026-09-05):** the owner IS `naufalazhar652952` (also known as `naufalazhar65` on Telegram and display name `Zigen` on Discord). `Zigen` is NOT a new/other user — it maps to the owner's data (`apps/web/.data/users/naufalazhar652952/`). When testing/knowing the owner, prefer `user:"naufalazhar652952"` or the channel's own key; do not mistake `Zigen` for a separate account.

- Choose provider via `NEXT_PUBLIC_AI_PROVIDER` (`mock` default | `groq`) for the voice pipeline, and `DEFAULT_AI_PROVIDER` (+ `NEXT_PUBLIC_DEFAULT_AI_PROVIDER`) for the default LLM provider when a request/channel doesn't specify one — code fallback is `groq`, `.env.local` sets `9router`. Set `GROQ_API_KEY` in `apps/web/.env.local` (server-side only!) for real voice. See `apps/web/.env.example`.
- `GroqStreamingProvider` (`src/ai/GroqStreamingProvider.ts`) implements `AIProvider` and drives: Whisper ASR → Qwen streaming SSE → Orpheus TTS per sentence.
- `VoiceActivityDetector` (`src/audio`) does energy-based VAD on the live mic; `AutoTurnManager` (`src/ai`) runs the gated hands-free loop — VAD is active in **LISTENING** (records utterances, 1s anti-feedback cooldown) and in **SPEAKING** in "barge" mode (higher threshold + 260ms hold, relies on mic echoCancellation) to trigger a voice interrupt over AI audio. The manual Interrupt button also does FR-007.
- Room: `GROQ_VOICE` (autumn/diana/hannah/austin/daniel/troy). Orpheus is **English + Arabic (Saudi) only**; Indonesian now uses a **local macOS voice** (`say -v Damayanti` via `ttsLocal.ts`, `TTS_LOCAL_ID=0` to disable) instead of English pronunciation. Partial STT remains Phase 3.
- Groq STT/TTS are request-based (no WebSocket streaming) — transcripts are final-only. This is why the API is proxied through Next routes holding the key.

Note: Next.js detected a stray `package-lock.json` in `$HOME`; `outputFileTracingRoot` is set in `apps/web/next.config.js` to silence it. Do not remove.

## Critical architecture decisions (from PRD)

- **AI Provider must be abstracted** via `AIProvider` (`connect`, `sendAudio`, `sendText`, `interrupt`, `disconnect`). UI/audio never couple to a specific provider. Swap by changing the provider instance only.
- **Conversation state machine** is explicit: IDLE → LISTENING → PROCESSING → SPEAKING → (TURN_END → LISTENING | INTERRUPTED → LISTENING). Real state machine, not ad-hoc booleans.
- **ConversationManager** (`apps/web/src/ai/ConversationManager.ts`) is the only place that forwards provider events into the state machine. UI talks to it, not to providers.
- **Audio pipeline** (§14): Microphone → Capture → Noise Suppression → VAD → Streaming → ASR → Conversation Manager → LLM → TTS → Streaming → Speaker. VAD and real audio streaming are Phase 2/3.
- **MVP latency targets** (§16): mic→processing <100ms, user stops→AI starts <500ms, first AI audio <1.5s, interruption detection <200ms.

## Key invariants to preserve

1. AI Provider abstraction — swap providers without changing UI/audio
2. State machine correctness — invalid transitions impossible (`transition` returns null)
3. Interrupt safety — old generation must not interfere with new turn (providers must abort streams on `interrupt()`; see MockProvider `generationId` and GroqStreamingProvider `generationId` + `AbortController`)
4. Latency budget — every new feature must consider its impact on the <1.5s first-audio target
5. Security — API keys server-side only (they live in `/api/*` route handlers), tool calls validated server-side, no client-side privileged calls

## Gotchas

- MockProvider is synchronous; `sendText`/`sendAudio` stream the canned answer via `setInterval`. Do not rely on its timing in tests.
- The mic permission/audio path (`AudioCapture`) requires a browser; it degrades to `denied`/`error` states gracefully.
- `AudioPlayer` decodes WAV chunks queued per sentence; `pump()` is guard-locked (no concurrent playbacks) and fires `onDrained` when the queue empties. `TURN_END` is emitted only after playback drains (UI calls `ConversationManager.finishTurn()`), so long replies never re-open LISTENING while the tail is still spoken.
- vitest runs the state-machine tests (`npm test` = `vitest run`). The two `verify.ts` proofs are kept and still run via tsx.
- Next.js route files may only export valid Route fields — do not export helpers/constants from a `route.ts` (build fails).

## Plugin / tool system (Fase 4, 2026-09-04) — typecheck/lint/build/test green

`apps/web/src/lib/tools.ts` is now a **plugin registry** instead of a flat `TOOLS` array + `executeTool` switch that could drift apart. Core types: `ToolDefinition` (schema, unchanged), `ToolRisk` (`read`/`write`/`delete`/`transaction`/`external`), `ToolContext { userKey, rawUser }`, and the new `ToolPlugin { definition, execute(args, ctx) }`. The registry `toolRegistry` bundles each tool's schema + implementation in ONE object. Public exports are all **derived from the registry** so they can't diverge:
- `TOOLS = toolRegistry.map(p => p.definition)` — what's sent to the model (agent.ts uses `TOOLS`, and `TOOLS.find(t => t.function.name === c.name)` for `requiresConfirmation`).
- `executeTool(call, rawUser)` — parses JSON args, looks up `getTool(call.name)`, dispatches to `plugin.execute(args, { userKey, rawUser })`, returns `Error: ...` on unknown tool. Per-tool error handling lives inside each plugin's `execute` (preserving the old switch's exact per-case try/catch).
- `registerTool(plugin)` — add/replace a tool at runtime (idempotent by name).

**Adding a new tool = adding one `ToolPlugin` object** (definition + execute) to `toolRegistry` — no separate switch, no list to keep in sync, and it's automatically visible to the model (`TOOLS`) and dispatchable (`executeTool`). **276 tools currently registered** (verified 2026-09-15 via `getTOOLS().length`; the full list is derived from `toolRegistry` → `TOOLS`, never hand-kept). Note: module-level helper functions (webSearch, evaluateArithmetic, fileRead, fileWrite, fileEdit, saveNote, listNotes, deleteNote, scheduleReminder, fetchUrl, searchMemory, addAutomation, ...) are declared later in the file and referenced by the plugin closures — fine because they run at call-time. `verify.ts` still exercises `executeTool` for calculate/save_note/list_notes/delete_note/file_read/exec/write_file/edit_file/memory_get/browser/device/calendar (green). Kept server-side only (imports `node:fs`).

**Device nodes (Fase 4, 2026-09-04):** `apps/web/src/lib/devices.ts` — minimal safe macOS node, per-user paired devices at `.data/users/<user>/devices/devices.json` (max 10, via `DEVICE_SECRET` env, `pairDevice`/`listDevices`/`getDevice`). Tools: `device_list` (read, no secret), `device_exec`/`device_screenshot` (write, FR-014, capability-checked, allowlisted `ls/pwd/cat/git` etc., `screencapture -x` on Mac). `ensureLocalDevice` auto-creates `local-mac` when no secret. `POST /api/devices` for pairing, `GET` for listing. Verified `device: OK` in `verify.ts`.

**Browser automation (Fase 4, 2026-09-04):** `apps/web/src/lib/browser.ts` Playwright headless (chromium, 1280x800, no-sandbox, image/media/font blocked). Tools: `browser_open` (read, SSRF-guarded, http(s) only), `browser_snapshot` (read, ARIA tree walk), `browser_click`/`browser_type`/`browser_navigate` (write, FR-014 confirm). Single browser+page per process (personal deploy). Verified `browser: OK` (tools registered) in `verify.ts`.

**Daily memory (Fase 4, 2026-09-04):** `apps/web/src/lib/dailyMemory.ts` stores per-user, per-day markdown at `.data/users/<user>/memory/YYYY-MM-DD.md` (also `today`/`yesterday` aliases) via `appendDailyMemory`/`readDailyMemory`/`listDailyMemories`. `agent.ts` appends the last user+assistant snippet on every turn (fire-and-forget, best-effort) for both groq/9router and opencode paths. `rag.ts` indexes daily memory files (`memory:YYYY-MM-DD.md`) into `search_memory` BM25. Tool `memory_get` (risk `read`, auto) retrieves a day's log. Verified in `verify.ts` (write via helper, read via tool, `today` alias, miss on empty).

**Heartbeat (Fase 4, 2026-09-04):** `apps/web/src/lib/heartbeat.ts` periodic check-in every `HEARTBEAT_INTERVAL_MINUTES` (default 30m, 0=off, env) — scans all users' active tasks with `dueAt` for overdue (past due) and due-soon (within 1h), pushes a 💓 heartbeat summary via `pushToOwner` when pending, silent otherwise. Started in `instrumentation-node.ts` alongside `automationRunner`. `runHeartbeatTick`/`stopHeartbeat` for tests. Verified in `verify.ts` (tick no-throw when nothing pending).

**Webhook (Fase 4, 2026-09-04):** `POST /api/webhook` external trigger — body `{secret, prompt, user?, provider?}` → `runAssistantTurn` + `pushToOwner` (🔔 webhook) + JSON `{status, text, pushed}`. `WEBHOOK_SECRET` env guards when set (401 on mismatch); `GET` returns usage. `WEBHOOK_USER`/`WEBHOOK_PROVIDER` defaults. Verified via curl (needs_confirmation for write tools, ok for read).

**`exec` read-only shell tool (Fase 4, 2026-09-04):** a new tool (`risk: read`, auto-runs, no FR-014 confirm) that runs a safe inspection command and returns output. Strict allowlist in `tools.ts` (`EXEC_ALLOWLIST`): git (only read-only subcommands status/log/diff/branch/ls-files/show/rev-parse/--version), ls, pwd, cat, node (--version/-v), npm (ls/--version). `EXEC_FORBIDDEN_ARG` rejects `;`/`&&`/`|`/`>`/`<`/`..`/`$(`/backtick, and `EXEC_FORBIDDEN_SRC` blocks paths matching `.env`/`.local`/keys/`node_modules`/`.next`/`.data`/`package-lock`. Runs via `execFile` (no shell interpretation) in `resolveInSandbox(cwd)` (defaults to `repoRoot()`) with a 10s timeout and 60KB output cap; `cwd` may be an absolute path inside an allowed workspace (multi-root sandbox). Verified in `verify.ts` (git status/ls/cat/pwd OK; rm -rf, git push, node -e, pipelines, escapes, too-many-args rejected; multi-root cwd OK).

**Multi-workspace sandbox (Fase 4, 2026-09-04):** `apps/web/src/lib/users.ts` now exports `allowedWorkspaces()` (from `ALLOWED_WORKSPACES` env, comma-separated absolute paths), `sandboxRoots()` (`[repoRoot, ...allowedWorkspaces]`), and `resolveInSandbox(path)` (accepts both relative paths like `README.md` and absolute paths inside an allowed root like `/Users/me/PROJECT/other-app/src`). Both `file_read` and `exec` (`cwd`) use `resolveInSandbox` — a request escapes every root → `Error: path escapes every allowed sandbox root`. `file_read`/`exec` also keep their existing `DENY_SEGMENTS`/`DENY_PATTERNS`/`EXEC_FORBIDDEN_*` guards. `.env.example` documents `ALLOWED_WORKSPACES`; `.env.local` sets it to `flowtest-studio`. `verify.ts` exercises the multi-root sandbox with a temp workspace (absolute resolve succeeds, `../` and `~` and outside paths are rejected).

**Cross-channel `send_channel` (Fase 4, 2026-09-04):** tool (risk `read`, so no FR-014 confirmation — it relays immediately) that sends a message to another registered channel (Telegram ↔ Discord). Backing store is `apps/web/src/channels/pushTarget.ts` — now a **label-keyed registry on `globalThis`**, NOT a module singleton. Reason: bots register from `instrumentation-node.ts` while tools dispatch from route/bundle code, and Next compiles these into distinct webpack modules — a module-level singleton gives each a separate `senders` map, so `send_channel` saw an empty registry even though the bots logged "target registered". Storing the map on `globalThis` (one shared Node process) unifies both bundles. Gotcha: the Discord/Telegram sender targets the owner's last-seen channel, which is only recorded once the owner sends a message since server start — so `send_channel` to Discord fails with "no discord owner channel seen" until the owner chats there first (same as reminder push). Imports of pushTarget are intentionally **relative** everywhere (tools.ts/agent/discord/telegram/automationRunner) so tsx probes resolve; the globalThis state is what guarantees cross-bundle correctness.

**Travel & live data (2026-09-12):** `waze.ts` (Waze Direct free, Nominatim → `routing-livemap-row` XML retry 3× → OSRM fallback, `waze.sh`/`waze_route` tool read auto, human + OSRM baseline), `weather.ts` (wttr.in j1 + Open-Meteo fallback, `weather.sh`/`weather` tool), `hotel.ts` (Playwright Booking.com `id-ID`, `hotel.sh`/`hotel_search` tool read auto, 60–150% band). All `risk: read`, auto, no key.

**Git helper + SafeExec (2026-09-12):** `git.sh` (`add -A` + commit + push, `--status`) + tools `git_status` (read auto) / `git_commit` (write confirm) + `safeExec.ts` (CRITICAL/HIGH/MEDIUM/LOW, `~/.openclaw/safe-exec/pending/req_*.json`, audit `safe-exec-audit.log`, env `SAFE_EXEC_DISABLE`/`OPENCLAW_AGENT_CALL`/`SAFE_EXEC_AUTO_CONFIRM`, bash `safe-exec*` → `~/.local/bin`, tool `safe_exec_list` read auto, guard in `exec`/`exec_write`). Probes `*_probe.ts` gitignored.

**CUA native GUI (2026-09-13, maksimal):** `cua.ts` wrapper `cua-driver serve` (daemon) + 10 tools: `cua_doctor`/`cua_list_apps`/`cua_window_state` (read auto, snapshot invariant WAJIB), `cua_launch`/`cua_click`/`cua_type` (write confirm, AX vs px), `cua_start_session` + `cua_browser_state`/`cua_browser_click`/`cua_browser_type` (typed Chromium per `BROWSER.md`, exact window→tab→ref). Companion docs `~/.openclaw/workspace/skills/cua-driver/` (`SKILL.md` core + `MACOS.md`/`BROWSER.md`/`WINDOWS.md`/`LINUX.md`/`RECORDING.md`). Verify `cua_doctor` OK, native AX/PX ladder per `SKILL.md`. **Pentest Copilot (2026-09-14, LEGAL/authorized only):** owner works in cybersecurity — persona facts `job`/`interest` recorded. Prompt adds the pentest workflow (Recon→Enum→Vuln→Exploit→Post→Report), OWASP Top 10, report format, and a HARD scope rule (own lab/localhost/authorized only; platforms forbid automation). `labs/pentest/docker-compose.yml` = local vulnerable lab (Juice Shop :3001, DVWA :8081, WebGoat :8082, 127.0.0.1-only; Docker prerequisite). Tools: `pentest_resources` (platforms+lab+scope), `pentest_scan` (nmap/nuclei/nikto/ffuf — `isLabTarget()` REJECTS public; write/confirm; install hint when bin missing), `finding_add`/`finding_list` (per-user findings.json), `report_generate` (markdown report). verify covers isLabTarget (public rejected) + findings/report. **Ethical-Hacker toolkit (2026-09-14, keyless, defensive/authorized only):** `security.ts` + tools `security_scan` (macOS posture: FileVault/Firewall/Gatekeeper/SIP/listening ports + score), `secret_scan` (walk a sandbox dir for AWS/GCP/Slack/OpenAI/GitHub tokens, private keys, JWT, generic secret assignments — reports file:line:type only, VALUES REDACTED), `tls_check` (node:tls cert issuer/expiry/chain for a host), `breach_check` (HaveIBeenPwned Pwned Passwords k-anonymity, keyless — only the 5-char SHA-1 prefix leaves; args redacted in auditLog). All `risk: read` (auto). Boundary: OWN/authorized/engagement-scoped only. Full cheat-sheet: `SECURITY.md`. Lanjutan (2026-09-14): `engagement_create/list/close` (otorisasi klien + guard scope-aware `targetAllowed`), `cvss_score` (CVSS v3.1), `finding_resolve`, `finding_export` (CSV/JSON/**SARIF**), `dep_audit` (OSV, npm/pypi, fixed-version) + `verify_patch` (auto-resolve), `hardening_plan`/`hardening_pdf`, `report_save/pdf` (header engagement), `lab_status/start/fetch` (lab no-Docker `labs/pentest/vuln-node` :4010 — 13+ kelas kerentanan), `security.ts`/`securityWatch.ts` (heartbeat: port baru/cert expired/engagement <24h). Total tools now 246 (Tier-A: `encoding`, `http_request` API lab-scoped, `trivy_scan`, recon `whatweb`/`gobuster`, exec `tcpdump -r`/`nc -z`/`searchsploit`, finding pro fields steps/root_cause/references). **Strix-adapted playbooks + white-box/recon (2026-09-15):** vendored 78 pentest knowledge packs (adapted from Strix, Apache-2.0) at `apps/web/security-playbooks/<category>/*.md` across 11 categories — **methodology** (application-security-testing, owasp-top-10-testing, api-security-testing, whitebox-code-review, fix-and-verify, source-aware-whitebox), **scan_modes** (scan-modes: quick/standard/deep/diff), analysis (counterevidence, severity-calibration, fix-verification, source-aware-discovery), vulnerabilities ×28 (ssrf, idor, xss, sql_injection, ssti, xxe, csrf, race_conditions, http_request_smuggling, authentication_jwt, mass_assignment, path_traversal, nosql_injection, insecure_deserialization, prototype_pollution, business_logic, subdomain-takeover, llm-prompt-injection, …), tooling (nmap, nuclei, httpx, ffuf, sqlmap, subfinder, katana, naabu, semgrep, hurl, python, agent_browser), cloud (aws, azure, gcp, kubernetes), frameworks (nextjs, django, fastapi, nestjs), technologies (llm-applications, active_directory, auth0, firebase, supabase, grafana_prometheus, electron_desktop_apps), protocols (oauth, graphql), reconnaissance (asset_discovery, infrastructure-lifecycle), custom (source-aware-sast, dependency_cve_scanning, api_spec_testing, npx_confusion) — loaded on demand by the `security_playbook` tool (read/auto; no args = catalog). Adds `sast_scan` (semgrep `p/default`+`p/secrets` on a sandbox dir, read/auto, install-hint, needs net on first rules download) and `recon_takeover` (passive CNAME-fingerprint takeover candidates over cached subdomains, DNS-only, read/auto). Prompt now mandates the counterevidence → severity-calibration → fix-verification passes before `finding_add`/`report`. **Bug-bounty support (2026-09-15):** public programs (HackerOne/Bugcrowd/YesWeHack/Intigriti) are authorized in-scope (safe harbour) — Mia asks for in-scope/out-of-scope + policy URL, recommends `engagement_create` (authorization=policy URL), defaults to **manual + rate-limited** (no automated scanners unless RoE allows), refuses DoS/other-users'-data/social-engineering, then `finding_add` → `report_*`. `pentestResources()` distinguishes bug-bounty (authorized) from training platforms (no automation). **Bounty toolkit (2026-09-15):** `oast.ts` — `oast_create`/`oast_poll`/`oast_stop` (webhook.site keyless OOB callback; proves blind SSRF/XSS/XXE/RCE; per-user `.data/users/<u>/oast.json`); `httpSession.ts` — `http_session` named cookie/header store (`.data/users/<u>/http-sessions.json`) + `http_request` gains `session`/`save_session`; `bola_diff` (same request, two sessions A/B → identical-200 BOLA/IDOR signal); `content_discover` (robots/sitemap/page links/JS endpoint mining + bounded common-path probe). All scope-gated via `targetAllowed`; OAST/session are read/auto, bola_diff/content_discover write/confirm. **+ (2026-09-15):** `param_fuzz.ts` (`param_fuzz` — injects XSS/SQLi/SSTI/redirect/cmdi payloads per param, flags reflection/SQL-error/SSTI-eval/redirect/timing; optional `callback` adds SSRF; `classify()` pure-tested), `jwt.ts` (`jwt_attack` — decode/forge alg:none/HS256/alg-confusion/crack weak secret; local crypto, read/auto), `evidence.ts` (`evidence_capture` — raw HTTP + Playwright screenshot to `reports/evidence/`, write/confirm). **Recon/crawl completion (2026-09-15):** `scopeImport.ts` (`scope_import` — heuristic parse of program Targets text/url → in/out-of-scope + `engagement_create` suggestion; `parseScopeText` pure-tested), `recon.ts` adds `crawl` (bounded same-origin BFS), `recon_diff` (new-asset diff vs cache, passive), `recon_screenshot` (visual recon, write); `paramFuzz.ts` adds `param_discover` (~100 common names). CORE stays 128 (5 tools added; 5 niche daily tools demoted — still on opencodego/all-245). **Advanced bounty tools (2026-09-15, tools 229):** `requests.ts` (`request_save`/`request_run` — saved request templates + `{{var}}` substitution, runs via `httpRequest` so scope still applies), `apiSpec.ts` (`api_spec` — OpenAPI/Postman JSON → endpoints; `graphql_probe` — introspection), `recon.ts` adds `js_mine` (endpoints + secrets from JS bundles, values redacted; reuses `scanTextSecrets`), `security.ts` adds `platformFromCvss`/`platformSeverity` (CVSS → HackerOne severity + Bugcrowd VRT, also injected into `report_generate`) and `politeDelay` (env `SECURITY_REQUEST_DELAY_MS`) + `http_request` `user_agent`. No full CVSS v4 scorer yet (v3.1 remains). CORE stays 128 (6 added, 6 niche demoted). **Intel/recon/tracker (2026-09-15, 234 → 240):** `cveIntel.ts` (`cve_intel` — NVD keyword API keyless + optional `searchsploit`), `recon.ts` adds `recon_dnsbrute` (native DNS wordlist + wildcard detect, read), `recon_ports` (native TCP connect on common ports, scope-gated, write), `bucket_enum` (S3/GCS candidate probe, scope-gated, write), `submissions.ts` (`submission_track` add/list/update/delete + similar-title dedup hint, read). CORE stays 128 (5 added, 5 niche demoted). **Rapyd API support (2026-09-15, tools 241):** `rapyd.ts` (`rapyd_request` — computes the Rapyd HMAC request signature `BASE64(HEX(HMAC_SHA256(secret, lower(method)+path+salt+timestamp+access_key+secret+body)))`, sends via `httpRequest`; sandbox-only per RoE; secret args redacted) + `engagement.ts` `normalizeHost` now strips a leading `*.` so **wildcard scopes** (`*.rapyd.net`) authorize subdomains. CORE stays 128 (1 added, 1 demoted). `verify.ts` covers playbook load/search, takeover fingerprints, and sastScan graceful ENOENT. **Recon suite (2026-09-15, keyless):** `recon.ts` + tools `recon_subdomains` (passive Certificate Transparency crt.sh, hackertarget fallback), `recon_params` (passive public-archive OTX/urlscan/Wayback URL/param mining, flags id/redirect/url/file/dst), `recon_list` (per-user cache `.data/users/<user>/recon.json`), and `recon_httpx` (ACTIVE live-host HTTP(S) probe, scope-gated by `targetAllowed`, write/confirm). Passive sources open to any domain (public OSINT); active probing is lab/engagement/`PENTEST_LAB_TARGETS` only. Pure helpers `cleanDomain`/`parseCertNames`/`extractParams` are unit-covered in verify. **Desktop input maximized (2026-09-14):** added `cua_keys` (hotkey/press/type via cua-driver `hotkey`/`press_key`/`type_text`), `cua_mouse` (scroll/right_click/double_click/drag), `cua_pointer` (cursor position/screen size — read), and `clipboard_get`/`clipboard_set` (cua-driver clipboard) — so Mia matches OpenClaw desktop-control without pyautogui/OpenCV. cua_keys/cua_mouse/clipboard_set are write (FR-014 confirm); cua_pointer/clipboard_get read/auto. **Authenticated-app triage (2026-09-15):** `tamper.ts` (`tamper_script`, read/auto) generates a ready-to-paste Console script that patches `fetch`+`XHR` to rewrite a JSON request body in flight — the fix for "WAF/Cloudflare blocks curl and manual `fetch` (`Attention Required`)", so the app's own request (which passes CF) carries the tampered body. Two new playbooks: `methodology/browser-transport-tampering` (prosedur + tabel interpretasi) and `custom/idor-triage` (triage IDOR 2-akun; userId-in-body vs token binding: 401 = terkontrol, 200+data lain = temuan; mass-assignment pakai nama field ASLI dari GET). `bola_diff` now runs `jsonFieldDiff` (exported, pure) and names the differing leaves, flagging sensitive ones (`userid`/`accounttype`/`role`/`verified`/…) instead of just "bodies differ". Prompt gained an AUTHENTICATED APP TESTING rule. CORE stays 128 (`tamper_script` added, `calendar_mac_list` demoted). Verified in `verify.ts`. **Hunt agility (2026-09-15, tools 246→249):** `huntLog.ts` (`hunt_log`, read/auto) — durable per-user `.data/users/<u>/hunt-state.json` memory of what's been tested per target (status todo/testing/dead/lead/finding + note + evidence, upserted, cap 200, `normalizeTarget` pure-tested) so a hunt doesn't re-tread dead ends; two new orchestrators in `hunt.ts` (write/confirm, scope-gated, bounded + 150ms spacing): `auth_hunt` (probes login/register/forgot/reset/providers/csrf/openid → status/redirect/cookie-flag/CSP per path + leads) and `api_hunt` (OpenAPI/Postman spec → enumerate endpoints → unauth probe, flags sensitive paths answering 2xx; optional saved session). Prompt gained a HUNT AGILITY rule (check `hunt_log list` before testing; mark `dead` and auto-pivot to the next in-scope host without asking; stay non-intrusive). CORE stays 128 (3 added — `hunt_log`/`auth_hunt`/`api_hunt`; 3 niche Mac tools demoted: `reminders_mac_add`/`reminders_mac_list`/`calendar_mac_add`, still on opencodego). Verified in `verify.ts` (hunt_log round-trip + auth_hunt/api_hunt scope guard). **Bounty speed (2026-09-15, tools 249→251):** `suite_hunt` (hunt.ts) = one confirmation runs security_hunt + auth_hunt (+api_hunt when a spec/session is given), merges `extractLeads()` into one prioritized list **and auto-writes hunt_log** (lead|dead) — fewer round-trips. `engagement_targets` (engagement.ts) = worklist of all in-scope hosts of ACTIVE engagements, joined with hunt_log status (`readHunt`) so a session starts from a list and skips `dead`. `addFinding` now auto-attaches the latest matching `http_history` record as evidence when none is supplied (report picks it up). `securityWatch` now also auto-watches the ACTIVE engagements' root domains (≤6 domains, ≤2×/day via `scopeWatchAt` gate) and alerts on NEW subdomains (fresh surface). CORE stays 128 (2 in: `suite_hunt`/`engagement_targets`; 2 niche demoted: `calendar_check`/`automation_list`). Verified in `verify.ts` (suite_hunt guard + engagement worklist + finding auto-evidence). **Authenticated testing via CDP (2026-09-15, tools 251→255):** `cdp.ts` bridges to the USER's own Chrome over 127.0.0.1 (DevTools Protocol) — `cdp_status` (read/auto: list tabs), `cdp_request` (write: runs `fetch` INSIDE the scope-gated tab, so cookies + CF clearance apply and the WAF that blocks curl/headless is bypassed legitimately; `token_from` is an in-page expression evaluated at request time, so a Bearer token NEVER enters Mia's args/LLM), `cdp_eval` (write: read page state / apply a tamper patch), `cdp_open` (write: navigate). Setup: `scripts/chrome-debug.sh` (Chrome `--remote-debugging-port=9222` + dedicated `~/.mia-chrome` profile — Chrome ≥136 forbids the default profile). Uses Node's global `WebSocket` (no new dep). Each tab/URL is scope-gated via `targetAllowed`. Invariant 5 preserved: the secret lives in the browser, only the response returns. New playbook `methodology/authenticated-testing`; prompt AUTHENTICATED APP TESTING now leads with CDP, `tamper_script` as fallback. CORE stays 128 (`cdp_status`/`cdp_request` in; `gmail_read`/`google_news` demoted; `cdp_eval`/`cdp_open` on opencodego). Verified in `verify.ts` (cdp scope guards). **PoC harness (2026-09-15, tools 255→256):** `poc.ts` (`poc_verify`, write/confirm) turns a lead into proof before reporting — runs the request N× (default 3, max 8), fingerprints each response (status + sha256 body digest) to assert determinism, checks `expect_status`/`expect_contains`, and optionally diffs a `baseline_url` control; emits a report-ready verdict (STABIL/assertion-belum/TIDAK-stabil) and can save evidence. Prompt FINDING RULES now require a replayable lead pass `poc_verify` first. CORE stays 128 (`poc_verify` in; `dep_audit` demoted; still on opencodego). Verified in `verify.ts` (scope guard + deterministic 200 PoC + failed-assertion path). **Cloud + tech-change (2026-09-15, tools 256→259):** `cloud.ts` (`cloud_misconfig`, write/confirm) checks the org's own S3/GCS/Azure Blob listing, Firebase RTDB, and Supabase (keyless, bounded GETs); authorization is anchored to `base_domain` covered by an ACTIVE engagement (`engagementAllows`/`isLabTarget`), candidates derived via `cloudCandidates` (pure-tested) and classified by `classifyCloud` (pure-tested). `techWatch.ts` (`tech_watch`, read/auto) fingerprints a host from headers+markers (`detectTech`, pure-tested), diffs it against a per-user snapshot (`.data/users/<u>/tech-watch.json`), and runs `cveIntel` against changed tech — fresh surface = fresh bugs. Also in this commit: `nuclei.ts` + `nuclei_custom` (nuclei with severity/tags/custom templates, scope-gated, sandbox template path) — audited and fixed two defects: a non-ENOENT spawn failure was reported as "no findings" (now `Error: gagal menjalankan nuclei`), and space-separated tags were glued into one wrong tag (now `normalizeTags` splits on spaces/commas); regression tests added. CORE stays 128 (`cloud_misconfig`/`tech_watch` in; `research`/`read_upload` demoted; `nuclei_custom` on opencodego). Verified in `verify.ts`. **Autonomy & workflow (2026-09-15, tools 259→266):** `policy.ts` (`policy_set`/`policy_show`) — per-engagement auto-approve for a vetted subset; hard rules: only `read`/`write` risk (never delete/transaction/external), every URL in args must pass `targetAllowed`, inert without an active engagement; wired into `agent.ts` at the risky-call filter so an auto-approved call behaves like a read tool. `flow.ts` (`flow_run`/`flow_list`) — named multi-step sequences with `{{var}}` substitution, `extract` (json path / regex) and `expect_status`/`expect_contains` assertions (IDOR/logic proof); per-user `.data/users/<u>/flows.json`. `roi.ts` (`program_score`) — transparent heuristic scoring of a program scope + ranked host priorities (`scoreHost`/`rankTargets` pure-tested). `campaign.ts` (`campaign_run`) — bounded (≤12 hosts, ≥60s budget) resumable loop: suite_hunt per host, skips hunt_log dead/lead/finding, optional stop-on-lead; recon→hunt→triage only (no exploitation). `dupes.ts` (`dup_check`) — token-Jaccard similarity vs local findings+submissions (`similarity` pure-tested). CORE stays 128 (`policy_set`/`flow_run`/`campaign_run` in; `send_channel`/`gmail_search`/`codebase_refresh` demoted; the rest stay on opencodego). Verified in `verify.ts` (pure helpers + flow runner E2E against a local server). **One-command bounty (2026-09-15, tools 266→268):** `bounty.ts` (`bounty_run`, write/confirm) = single command draft-only pipeline: engagement (authorization) → ROI-ranked worklist (`roi`) → bounded resumable campaign → classify leads (`isHighSignalLead`, pure-tested) → **DRAFT** findings (label jelas "belum diverifikasi", + `dup_check`) → draft report → handoff list. Hard boundaries: **never submits** to a program, never destructive, never bypasses a WAF; state `.data/bounty-state.json` (history cap 10) + `bounty_status`. `campaign.ts` refactored to export `campaignRunDetailed` (structured host results) + `huntStatusFor` (normalizeTarget-keyed, regression-tested). Verified: live E2E on a local server (cookie-without-flags → 1 candidate → draft finding + report); scope/guard paths in verify. **OAuth + writeup (2026-09-15, tools 268→270):** `oauth.ts` (`oauth_hunt`, write/confirm) fetches OIDC discovery then probes the authorization endpoint with `redirect_uri` bypass variants (`redirectVariants`/`classifyOauthRedirect` pure-tested) → detects off-host redirect (ATO path); `writeup.ts` (`writeup`, read/auto) renders a submission-ready, impact-first markdown report from a finding + its evidence (`renderWriteup` pure-tested). CORE stays 128 (`oauth_hunt`/`writeup` in; `plan_get`/`gmail_list` demoted).

**Health Tracker (2026-09-13, no bugs):** `health.ts` per-user `health.json` (water/sleep, `sanitizeUser` + `tmp→rename` atomic, cap 500, validasi cups 1–20), tools `health` (read auto, no confirm, action `water`/`sleep`/`wake`/`stats`/`update`/`delete`), prompt `minum 2 gelas`/`mau tidur`/`bangun`/`statistik` → `Tercatat …`/`Hari ini: X gelas`.

**Clawic Memory (2026-09-13, maksimal mandiri):** `clawicMemory.ts` durable `.memory/` (INDEX capped 100, entry `Keywords`/`Facts` dated+sourced/`History`, one fact one home, write before reply, `never store secrets`), tools `memory` (read auto, `remember`/`recall`/`forget`/`stats`), `ingat ini: Alice pindah` → `people/alice-smith.md` + `INDEX`.

**Reminder aneh fix (2026-09-13):** `reminderMessage.ts` BODIES `["{text}", "Beb, {text} 🌸"]` + TAILS `["", "Semangat ya beb 🌸"]` + `· pukul HH:MM` (was random `aku ingetin lagi: {text}, pelan-pelan aja.` + `jangan lupa ya hehe` double). Push `🌸 Mia — ${reminderMessage}` now single natural line, no double.

**Reminder honest delivery audit (2026-09-14):** a due slot is only marked delivered when a listener actually pushed it to a real target — every listener radio-acks synchronously (`ReminderListener` returns `boolean`). No ack (device off, channel not yet recorded) → the slot is **never burned**: it stays due as `missedAt` + `delivered:false` and retries on the next connect, so Mia can truthfully say "tadi kelewat" (bukan "(terjadwal)" palsu). Daily acked → reschedule +24h with `lastFiredAt`/`deliveredAt` recorded; `deliveredAt − lastFiredAt` gap >30min surfaces as "kesampaian TELAT … pas device off" (late replay after a boot), so a replayed wake is never claimed on-time. `reminders_list`/`buildReminderList` print `· terkirim`/`· KELEWAT`/`· kesampaian TELAT`; REMINDER HONESTY prompt tells Mia to own a miss + offer a resend. SSE stream now subscribes BEFORE `takeDueReminders` (replay is acked, no double-push). Exactly-once holds per **delivered** slot; a skipped slot is never claimed delivered. Gates: typecheck/test 9/9/verify EXIT=0 (52 OK: +`reminder missed delivery`, +`reminder late delivery`). Commit `59c91a6`. Residual: ack is optimistic (counts when a send is initiated; a failed send logs but still counts as delivered).

**Auto-Update Mia (2026-09-14, mandiri):** `autoUpdater.ts` daily `04:00` WIB (Asia/Jakarta, `AUTO_UPDATE_HOUR`, 0=off) runs the full loop: worktree-dirty check → `git pull --ff-only` → `npm install` → gates `typecheck`+`test`+`verify` → pushToOwner summary (Telegram+Discord) → delayed restart (detached bash after 8s). State atomic `.data/auto-updater/state.json` (history cap 10, lock anti-double-run, dedup once/day via `lastRunDate`, `AUTO_UPDATE_TICK_MIN`=5m + one boot check 90s in). Red gates → update rejected + `git reset --hard <before>` suggestion. Knobs `AUTO_UPDATE_*` (ENABLED/HOUR/GRACE_MIN/TICK_MIN/REMOTE/BRANCH/NPM/VERIFY/RESTART/DELIVER/TIMEOUT_MS). Tools `auto_update_status` (read auto) + `auto_update` (write confirm). Wired in `instrumentation-node.ts`. Committed `2de61ee`.

**Self-improving agent (2026-09-12, maksimal, mandiri 2026-09-13):** `.learnings/` (flat) + `.self-improving/` (tiered HOT `memory.md` ≤100 + `corrections.md` + `projects/`/`domains/`/`archive/`, gitignored, also mirrored to `~/self-improving/` for OpenClaw) captures corrections/insights/knowledge gaps (LRN), command failures (ERR), and missing capabilities (FEAT) with `Pattern-Key: area.symptom` dedup + `Recurrence-Count`. Auto-log via `apps/web/src/lib/learnings.ts` (`logLearning`/`logError`/`logFeatureRequest`) wired into `agent.ts` `logCorrection` (user says "salah…") and tool `Error:` paths; Mia tools `learnings_search`/`learnings_review` expose it (`risk: read`, auto). Tiered `~/self-improving/` + flat `.learnings/` hybrid with local-first fallback (`ai-assistant/.self-improving` → `~/self-improving` → `.learnings`), `heartbeat.ts` Never-Forget checkpoint now checks local `.self-improving/memory.md` first (mandiri, no `.openclaw` hard dependency). Hooks: `~/.openclaw/hooks/self-improvement/` (global) + local `.self-improving` (mandiri). Promotion: `Recurrence>=3` in 30 days → `AGENTS.md`/`SOUL.md`/`TOOLS.md`.

## Session handoff (2026-09-02) — all 4 features DONE

User selected 4 features in order: FR-015 persisted conversation history (DONE) -> tool calling + web search (DONE) -> partial transcript (DONE) -> barge-in (DONE).

FR-015 DONE (typecheck/lint/build + both tsx verifies green, graph updated): groundwork `ConversationManager.serialize()` + `restoreHistory(transcripts)` (apps/web/src/ai/ConversationManager.ts) plus localStorage wiring in apps/web/src/hooks/useVoice.ts — hydrate on mount from `voice-ai.history.<user>` (name read from localStorage `voice-ai.user`), `manager.restoreHistory(saved.transcripts)` when any final transcript exists, and persist `manager.serialize()` on every transcript event. Helpers `historyUser`/`loadHistory`/`saveHistory` keep persistence best-effort (guarded try/catch) and key `voice-ai.history.<user>`. Next up: tool calling + web search.

FR-013 tool calling + web search DONE (typecheck/lint/build + verify green, graph updated): agent loop lives server-side in `/api/llm/route.ts` — first completion stays `stream:true` for fast TTFT; if Groq emits `delta.tool_calls` the route executes them in `apps/web/src/lib/tools.ts` (`TOOLS` = `web_search` + `calculate`) via `executeTool`, appends `assistant(tool_calls)` + `tool` messages, and follows up with another streamed completion, up to `MAX_TOOL_ROUNDS` (last round omits `tools` to force a text answer). Client sees only the final text (never tool deltas). `web_search` = DuckDuckGo Instant Answer API then HTML scrape fallback (`parseResults`, no key); `calculate` = safe recursive-descent arithmetic parser (no eval, whitelist charset). No FR-014 confirmation UI yet (tools are read-only; WRITE/DELETE/TRANSACTION need confirmation later). `verify.ts` extended with an offline calculator section. Known residual: DDG HTML POST may require a `vqd` token in some regions (Instant Answer covers many queries); tool-call turn latency adds ~1-3s. Next up: partial transcript.

FR-016 partial transcript DONE (typecheck/lint/build + verify green, graph updated): real-provider path feeds live partials. `AudioCapture.startRecording()` now uses a 250ms `MediaRecorder` timeslice and exposes `recordedChunkCount` + `snapshotRecording()`; `AutoTurnManager` gains an optional `onPartialAudio` callback that every 1200ms snapshots the growing utterance (skipping when no new chunks) and feeds `ConversationManager.sendPartialAudio` → `AIProvider.sendPartialAudio?` → `GroqStreamingProvider.sendPartialAudio` (Whisper on the partial blob, emits `partial_transcript`). `ConversationManager` only accepts partials in LISTENING (drops late partials racing the final) and treats a partial arriving after a final as a new utterance. Transcript UI already rendered partial state. MockProvider already emitted partials (QA path unchanged). Known residual: each utterance costs ~1 partial STT call per 1.2s of speech + 1 final (Groq free-tier Whisper RPM); truncated-WebM snapshots may occasionally transcribe as empty.

FR-007 voice barge-in DONE (typecheck/lint/build + verify green, graph updated): `AutoTurnManager` is now mode-based (`idle`/`listen`/`barge`). `useVoice` calls `autoTurn.armBargeIn()` on **SPEAKING** (and `stop()` on PROCESSING/INTERRUPTED, `start()` on LISTENING). In barge mode the same VAD runs with a higher threshold (0.03) + 260ms startHold + 500ms endSilence and relies on the mic's `echoCancellation`; detected speech fires `onBargeIn` → `ConversationManager.interrupt()` → INTERRUPT → provider abort (`generationId` + `AbortController`) → `interrupted` → RESUME → LISTENING, discarding any partial assistant output (`activeAssistantEntry = null`). `lastSpeechEndAt` is reset on arm so an immediate early barge-in isn't blocked by the 1s anti-feedback cooldown. Known residual: energy-based barge-in is tuned for mic AEC — very loud speakers may occasionally false-trigger; PROCESSING-phase barge-in still uses the manual Interrupt button only. All 4 selected features (FR-015, FR-013, FR-016, FR-007) are now DONE.

UI (chat input): the old mic-button + input form was replaced with a shadcn-style `PromptInput` in `apps/web/src/components/ui/ai-chat-input.tsx` (expanding composer: morphing placeholder, model/effort selectors, voice dictation via Web SpeechRecognition, image attachments + gallery modal). This added `apps/web/src/lib/utils.ts` (`cn`), deps `clsx`/`tailwind-merge`/`tailwindcss-animate`, a dark shadcn token set (`--background`, `--card`, `--muted`, …) in `apps/web/src/app/globals.css`, and their Tailwind color mapping + `duration-400` in `apps/web/tailwind.config.js`. Page layout: `h-dvh` column, header + compact orb + `flex-1 min-h-0` auto-scrolling transcript + fixed footer holding the "Start voice"/"Stop voice" pill (`toggleMic`, drives the hands-free VAD loop) and the `PromptInput` (submit → `sendText`). Known residual: the PromptInput's own mic (SpeechRecognition dictation) can overlap the hands-free VAD loop if both are used at once — use one mode at a time.

Orb: `Page.tsx` uses the WebGL shader `VoicePoweredOrb` (`apps/web/src/components/ui/voice-powered-orb.tsx`, deps `ogl`, shaders hoisted to module scope so the GL effect deps stay stable). `enableVoiceControl={isMicrophoneActive}` opens its **own** mic grab (echoCancellation:false, analysis-only — no feedback path) so it reacts to any audio incl. AI TTS; `hue` and a small state label are mapped per conversation state. `Button` (`components/ui/button.tsx`) + `@radix-ui/react-slot`/`class-variance-authority` added as shadcn primitives; old `VoiceOrb.tsx` deleted.

Tooling quirk: if file reads (Read/bash cat/sed/awk/grep) return `ccr://` handles that fail `caveman_retrieve` (cave_unknown_handle), restart the session — only exit-code/short outputs pass through.

FR-014 tool confirmation DONE (typecheck/lint/build + both verify green): risky tools now pause for explicit user approval. `apps/web/src/lib/tools.ts` `TOOL` defs gained a `risk` field (`read`/`write`/`delete`/`transaction`/`external`; `requiresConfirmation()` true for non-read); added `save_note` (risk `write`) writing to an in-memory array (no disk, demo-safe). The agent loop (`/api/llm/route.ts`) refactored: `runOneCompletion()` buffers each round and only streams the final answer; read-only tools (`web_search`/`calculate`) auto-execute server-side exactly as before (FR-013); if the model requests a **risky** tool on a fresh turn the route does NOT run it and instead returns a single control frame `@@CONFIRM <json>\n` as the whole body. Continuation is a second POST carrying `confirm_call:{call,allow}` (messages already include the assistant `tool_calls`); allow=true appends the executed tool result, allow=false injects "user declined this action; do NOT execute it". `GroqStreamingProvider` recognizes the `@@CONFIRM` prefix (never TTS's it), parses calls, stashes `pendingTools`, emits new provider event `tool_confirmation_required`, and gains `confirmTool(callId,allow)` which re-invokes the continuation. `ConversationManager` forwards `tool_confirmation`, exposes `confirmTool()`, stays in PROCESSING while awaiting. `useVoice` exposes `pendingConfirmation`/`confirmTool`/`denyTool`; `AIChatCard` renders an amber Confirm/Yes-No banner. `ConversationMessage.content` widened to `string|null` for tool_calls assistant messages. Residual: note store is in-memory; if LLM model lacks function-calling the risky path won't trigger.

Also on this session: (1) LLM endpoint is now configurable — `/api/llm/route.ts` uses `LLM_API_BASE`/`LLM_API_KEY`/`LLM_MODEL` (fallback to Groq); TTS stays on Groq `/api/tts`. `.env.local` currently has `LLM_API_BASE=http://localhost:20128/v1` + `LLM_MODEL=<open-code>` (a leftover proxy config — likely invalid; clear these to fall back to Groq unless the local proxy is running). (2) `useVoice` now calls `manager.connect()` on mount so typed chat works without mic; `ConversationManager.connect()` connects providers without entering LISTENING. (3) UI rebuilt as a floating `AIChatCard` (`components/ui/ai-chat.tsx`, 430→520px→full-height `w-[min(520px,94vw)] h-full`): orb moved inside the card header area via `orb` prop, voice/text output-mode segmented toggle (`voiceOutput` in `useVoice` mutes `audio_delta` and ends turns early when Text), user bubbles now white text on `bg-white/30`, assistant on `bg-white/10`. `FloatingParticles.tsx` uses a deterministic `mulberry32` PRNG (fixed hydration mismatch).

FR-009 voice selection DONE (typecheck/lint/build + both verify green): `GroqStreamingProvider` gained `setTtsSettings(voice)` (per-request `voice` in `/api/tts`); `/api/tts` route accepts+validates `voice` against Groq's 6 (autumn/diana/hannah/austin/daniel/troy, fallback `GROQ_VOICE`/hannah); `useVoice` exposes `voice`+`setVoice`; `AIChatCard` renders a pill selector under the orb. NOTE: `speed`/`pitch` were NOT added — verified via Groq docs that `/audio/speech` only accepts model/input/voice/response_format (Orpheus has no request `speed`); sending it errors. FR-010 multilingual PARTIAL (typecheck + live-verified, graph updated): `/api/tts` is now **language-aware**. Per-sentence heuristic detects Arabic script (U+0600–06FF + extensions) → `canopylabs/orpheus-arabic-saudi` with an Arabic voice (`GROQ_VOICE_AR` default `lulwa`; Arabic voices: abdullah/fahad/sultan/lulwa/noura/aisha); everything else (Indonesian, English, …) → `canopylabs/orpheus-v1-english` fallback (default `GROQ_VOICE`/hannah). Indonesian has NO Groq Orpheus model, so it is honestly TTS'ed in English pronunciation (fallback English confirmed by user). Verified live: Indonesian sentence → 200 audio/wav (RIFF PCM mono 24kHz 242KB); Arabic → routes to Arabic model but returns 502 `model_terms_required` until the user accepts the Arabic model terms in their Groq console (account gate, not a code bug). Residual: accept terms at console.groq.com to activate Arabic; true Indonesian TTS still needs a Groq multilingual model (FR-010 fully blocked at provider layer). Whisper/LLM remain multilingual; only spoken output is language-limited.

Provider selectable in Settings DONE (typecheck/lint/build + both verify green): the Settings panel (`components/ui/ai-chat.tsx`) now picks an **LLM provider** (Groq / OpenCode-local / 9Router / Mock) plus a model per provider. New `apps/web/src/lib/providers.ts` holds `PROVIDER_SPECS`, a client-safe `PUBLIC_PROVIDERS` (id/label/models only — never sections endpoints/keys), `isProviderId`, and server-only `resolveProvider(provider)` mapping id→(url,apiKey,defaultModel) from env: `groq`→`GROQ_API_KEY`+`GROQ_LLM_MODEL`; `opencode`→`OPENCODE_LLM_BASE`/`OPENCODE_LLM_KEY`/`OPENCODE_LLM_MODEL` (default `localhost:20128` / `open-code`, key optional `EMPTY`); `9router`→`LLM_API_BASE`/`LLM_API_KEY`/`LLM_MODEL`. `.git/` excluded. `mock`→no endpoint (route short-circuits: `/api/llm` streams a canned reply with zero network calls — token-free UI testing; `resolveProvider("mock")` returns `null` and is never called). `/api/llm` resolves via `resolveProvider` from the `body.provider` (default `groq`; unknown→`groq`), threads `url`/`apiKey`/`defaultModel` through `runOneCompletion`/`runAgent` replacing the old module-level `LLM_URL`/`LLM_KEY`/`MODEL` (removed). `GroqStreamingProvider` gained `setProvider(id)` and sends `body.provider`. `useVoice` exposes `provider`/`setProvider`/`providers`; switching provider resets the model to Auto. In the hook the live provider **instance** is `providerInstance` (the string state is `provider`) to avoid a name clash. Invariant 5 held: client sends only `{provider, model}`, never keys/endpoints. Key mapping lives in the table above; clear the stale `LLM_API_BASE=http://localhost:20128/v1` root-only config if you want `groq` to be the sole default (the old `LLM_*` envs now only feed the `9router` provider).

**BUG FIXED (ERR_EMPTY_RESPONSE on /api/llm):** the browser sent `provider:"groq", model:"open-code"`; the old route used a global `MODEL = process.env.LLM_MODEL ?? ...` = the leftover `LLM_MODEL=<open-code>`, so every default Groq request went out with model `open-code` → Groq 404 → `runOneCompletion` threw → `controller.error()` → the response aborted with empty body → Chrome logged `net::ERR_EMPTY_RESPONSE` (and a silent `error` event reached the UI). Root cause was the stale root `LLM_*` env bleeding into Groq. Fixed by the provider refactor: `defaultModel` is now per-provider (`groq`→`qwen/qwen3.8-27b`, independent of `LLM_MODEL`), and the client only sends `body.model` when a model is actually selected. Verified: `POST /api/llm` `{}` → 200; `{"provider":"groq","model":"open-code"}` → 000 (expected — Groq has no such model). Residual: typing/selecting a model a provider doesn't support still yields an empty-response fetch (the `error` event surfaces it); a custom/open misconfig (e.g. 9router→localhost:20128) behaves the same.

UI polish + test framework (session DONE): (1) **Mock provider in Settings** for token-free testing — `providers.ts` `ProviderId`/`PROVIDER_SPECS`/`PUBLIC_PROVIDERS` gained `mock` (models `[{id:"mock"}]`), `/api/llm` short-circuits `provider:"mock"` to stream a canned reply with zero network (`resolveProvider("mock")` returns `null`, never called). (2) **Bubble typing** — `@keyframes typing-dot` now bounces (`translateY`) + batches `.animate-typing-dot` with 0.15s/0.30s delays; AIChatCard dots use it; the typing `motion.div` bubble no longer pulses its own opacity (fade-in once). (3) **Smooth scroll** — transcript `scrollRef` effect uses `el.scrollTo({top, behavior:"smooth"})` + `scroll-smooth` on the container. (4) **Bubble entrance spring** — messages animate `{opacity,y:6,scale:0.96}`→`{1,0,1}` with spring `{stiffness:320,damping:28,mass:0.8}`. (5) **Assistant gradient bubble** — AI + typing bubbles use `bg-gradient-to-br from-white/15 via-white/[0.07] to-white/[0.03]` + `border-white/10` + `shadow-lg`. (6) **Error feedback** — `useVoice` exposes `lastError` (set on provider `error` event, cleared on next `text_delta`); `AIChatCard` renders a red `role="alert"` banner. (7) **Listening badge** — `AIChatCard` `isListening` prop renders a pulsing "Listening…" pill (wired from `state === "LISTENING"`). (8) **Settings persisted** — provider/model/voice hydrated from `localStorage["voice-ai.settings"]` (lazy state init via `loadSettings()`) and written on every change (`saveSettings`); a `useEffect` also re-syncs the hydrated values onto the GroqStreamingProvider instance. (9) **vitest added** — root devDep, `vitest.config.mts` (node env, `packages/*/src/**/*.test.ts`), root `test` script now `vitest run`; `packages/state-machine/src/index.test.ts` (9 tests: canonical loop, barge-in, reconnect, invalid→null, state preservation). Both `verify.ts` proofs still run via tsx and pass; typecheck/lint/build green. Note: dev server was found down (port 3000 closed → all routes `net::ERR_EMPTY_RESPONSE`/HTTP 000, not a code bug) and restarted.

FR-012 code input DONE (typecheck/lint/build/test green, graph updated): `PromptInput` already had multiline (`Shift+Enter`) + dictation, so code needs no new composer affordance. The gap was **rendering fenced code blocks in the transcript**: `apps/web/src/components/ui/ai-chat.tsx` gained `renderContent(text)` which splits message text on ` ```[\s\S]*?``` ` and renders each fence as a styled monospace `<pre>` (`bg-black/50`, `border-white/10`, optional language tag), keeping everything else inline — no markdown/Prism dependency. Used at both user and assistant bubbles (`{renderContent(msg.text)}`). Verified end-to-end via mock provider: a multiline turn with a JS fence `POST /api/llm` returned 200 and streamed the canned reply.

**DEV-SERVER GOTCHA (not code):** mid-edit hot-reloads corrupted `.next` webpack chunks → `Error: Cannot find module './819.js'` → **all** `/api/llm` (+ `_document`) routes 500 and returned an HTML error page, even `{"messages":[...]}` and mock. Fixed by `pkill -f "next dev"` + `rm -rf apps/web/.next` + fresh `nohup npm run dev` (Ready in 1369ms); groq-default and mock-code turns then returned 200. If routes start serving HTML 500s again, clear `.next` + restart dev rather than chasing a code bug. Streaming note: while an assistant answer streams a code fence it can render as raw partial text until the closing ``` streams in — acceptable since `partial` bubbles are faded.

Persistent notes + full FR-014 risk set DONE (typecheck/lint/build/test green, both verify proofs pass, graph updated): the `save_note` demo store is no longer in-memory — `apps/web/src/lib/tools.ts` now persists notes to disk at `apps/web/.data/notes.json` (fixed constant path, never derived from user input → no path traversal; atomic write via temp-file + `renameSync`; capped at `MAX_NOTES=50` / `NOTE_BUDGET=80000` bytes; `readNotes` swallows corrupt/missing files → safely returns `[]`). Added `list_notes` (risk `read`) and `delete_note` (risk `delete`), so all FR-014 categories (read auto-runs; write/delete pause for `@@CONFIRM`) are exercised by real tools. Dispatch added in `executeTool`; `/api/llm` SYSTEM_PROMPT tool list updated. `verify.ts` gained a save/list/delete round-trip assertion (uses the real on-disk store, incl. out-of-range delete guard). `tools.ts` imports `node:fs`/`node:path` statically (ESLint forbids `require`); it is only imported server-side (route `runtime="nodejs"` + tsx proof). Caveat: `.data/` is a runtime-write dir outside the Next build trace — fine for local dev, but a production deploy would need `outputFileTracingRoot`/volume handling for persistence across instances (out of MVP scope).

File access tool DONE (typecheck/lint/build/test green, both verify proofs pass, graph updated): Phase-4 "File access" filled via a read-only `file_read` tool in `apps/web/src/lib/tools.ts`. It is `risk:"read"` (auto-runs server-side, no FR-014 confirmation). Sandbox: resolved relative to `process.cwd()` (repo root), rejects absolute/tilde paths, and after `resolve()` normalization requires the result stay within root (blocks `..` escapes); a deny-list of **path segments** (`.git`, `node_modules`, `.next`, `.data`, `dist`, `coverage`) plus **filename patterns** (`.env*`, `*.local`, `.key/.pem/.crt`, compiled artifacts) keeps API keys and build dirs out of the LLM context (invariant 5/security). Files return content (truncated at `FILE_MAX_BYTES=60000`), directories return a `type\tname` listing (capped 200). Added to `executeTool` dispatch + `/api/llm` SYSTEM_PROMPT tool list. `verify.ts` + a standalone probe confirmed: reading source works, listing works, and `.env.local` (direct + via `..`), absolute `/etc/passwd`, `~/.ssh/...`, `node_modules`, and root escape are all blocked. Notes: workspace root is fixed to `process.cwd()` (the Next server / repo root) — a dedicated read-only docs dir could be made configurable later, out of MVP scope.

Per-user isolation (auth-lite) DONE (typecheck/lint/build/test green, both verify proofs pass, graph updated): persona + notes are now isolated per user, keyed by the existing `voice-ai.user` (from localStorage) sent from the client. New `apps/web/src/lib/users.ts`: `sanitizeUser()` (only `[A-Za-z0-9._-]`, ≤60, rejects `.`/`..`/empty — stops path traversal, invariant 5), `appRoot()`/`repoRoot()`/`userDataRoot()` (stable repo-root anchor so paths resolve identically whether the dev server runs with cwd=`apps/web` or the repo root — this fixed a latent cwd-dependent bug where notes/persona landed in `apps/web/apps/web/.data/`). `persona.ts` seeds a per-user persona at `apps/web/.data/users/<user>/persona/*.md` (copied from the shared `apps/web/persona` template on first use) and `loadPersonaPrompt`/`upsertPersonaFact` target it; `tools.ts` notes store per-user at `<user>/notes.json` (still falls back to shared `apps/web/.data/notes.json` when no user); `/api/llm` and `/api/persona` accept `user` and thread it through (incl. `executeTool(call, user)` in the agent loop and confirm continuation). `GroqStreamingProvider` gained `setUser(user)` and sends `user` in `/api/llm` + `/api/persona` bodies; `useVoice` syncs `historyUser()` onto the provider. Lib-to-lib imports use relative `./users` (not `@/lib/users`) so `verify.ts`/tsx proofs still run. Verified end-to-end: `city:Jakarta` written for `naufal` lands at `.data/users/naufal/persona/USER.md`, absent from `bob`s persona; sanitizer rejects `../evil`/spaces; global template not polluted. Note: this is identity (not authz) — anyone can claim any name; the gain is that facts/notes don't bleed across names. History stays per-user in localStorage.

TTS fallback (best-effort): `GroqStreamingProvider.speak()` no longer throws on TTS failure (429 rate limit, 5xx, network). It silently skips that sentence's audio while the assistant's text still streams to completion, so a throttled/expired Groq TTS quota (free tier 100 RPD) never surfaces as a raw 502 "Failed to load resource" or a hard error event to the UI. STT (`transcribe`) still reports errors (it's a live-input path the user should know about). Note: on 2026-09-02 the Groq TTS RPD limit (100/day) was exhausted → `/api/tts` returned 429 → proxied as 502 in the browser; this change makes that graceful.

Schedulers & reminders (Mia fitur) DONE (typecheck/lint/build/test green, both verify proofs pass, graph updated): per-user reminder store + scheduler + SSE push. New `apps/web/src/lib/reminders.ts` persists `{id,text,at,fired}` records per user at `.data/users/<user>/reminders.json` (sanitized user, atomic write, uncapped-at-40). `addReminder(text, atMs, rawUser)` and `takeDueReminders(rawUser)` (atomic mark-fired so each reminder delivers exactly once) + `subscribeReminders(listener)` feeding a lazily-started module-scoped interval (stop when no listeners). New tool `remind_me` (FR-014 `write` risk → confirmation) in `tools.ts` with `scheduleReminder()` that parses ISO-8601 `when` via `Date.parse`; added to SYSTEM_PROMPT. New `GET /api/reminders/stream?user=` SSE route (replays overdue on connect via takeDueReminders + live pushes + 20s keepalive; `cancel()` cleans up). `useVoice` opens an EventSource and exposes `reminders`/`dismissReminder` (queue capped 5); `AIChatCard` renders an amber "Reminder" banner with dismiss (Clock icon). End-to-end verified: due reminders fire as `data:` frames on connect and mark fired (reconnect delivers nothing). NOTES: (1) the scheduler is single-instance in-process (multi-instance deploys need an external queue — out of MVP); (2) a reminder that becomes due while NO stream is open stays unfired and is replayed on next connect (survives closed tab); (3) two serve-tests must use cwd-aware `appRoot()` anchoring (see users.ts) so paths match the dev server's cwd=`apps/web`.

**Recurring + varied reminders (2026-09-05, flood fix, typecheck/lint + reminder probe green):** `reminders.ts` `Reminder` now supports `repeat:"daily"`, `variants:string[]`, `variantIdx`. `addReminder(text, atMs, rawUser, {repeat?, variants?})` — a NEW daily reminder **merges into** an existing UNFIRED reminder at the same clock time (adopts/overwrites it and converts to daily). One-shots NEVER merge (two distinct one-shots at the same clock both fire). `takeDueReminders` now **reschedules a daily 24h ahead** (still unfired, `variantIdx+1`) and returns the rotated `variants[variantIdx % length]` text, while one-shots are marked fired — so "setiap hari jam 7" is ONE record firing daily instead of stacking duplicates. `reminderIntent.ts` detects `setiap hari/tiap hari/every day/daily/harian` → `repeat:"daily"` and `ganti(-)ganti/beda-beda/variasi/selang-seling` → `variants` (Mia-style pool). `agent.ts scheduleReminderFromIntent` + `tools.ts remind_me` (schema gained `repeat:"daily"` enum) thread opts through. **Root cause of the "notif banyak" flood:** the user asked repeatedly and every turn stacked a fresh one-shot at the next 07:00, so 4 reminders all fired at 07:00; opening the SSE stream or a chat then bursts all of them to Telegram+Discord owner. Fix = merge+reschedule above (probe-verified: 2 daily adds → 1 record; daily delivers A→B→C rotating, reschedules +24h; one-shot stays one-shot). Store cleaned for `naufalazhar652952` (now a single daily 07:00 wake with variants) and `naufal` (empty — was all test artifacts). **Multi-time compound asks DONE (2026-09-05):** `reminderIntent.ts` splits on clause boundaries (`dan`, `lalu`, `terus`, `,`) — "jam 1 siang ingetin makan siang, dan jam 8 malam ingetin makan malam" parses per-clause into TWO reminders (12:00 + 20:00), each carrying its own clause text; `parseClockTimes` (plural) extracts all clocks, `parseClockTime`/`detectReminderIntent` stay as single-first wrappers; `scheduleReminderFromIntent` loops the intents and joins the confirmation suffix ("pukul 12:00 dan 20:00"). Residual: a daily with no open stream at due time delivers once when a stream reconnects; multiple times inside ONE clause still resolve to that clause's first time.

BUG FIXED (TypeError on /api/llm confirm_call): calling a risky tool (remind_me/save_note/delete_note) that pauses for confirmation threw `Cannot read properties of undefined (reading 'id')` at route.ts and returned HTTP 500. Root cause was a client↔route contract mismatch on `confirm_call`: `GroqStreamingProvider` sent a flat `{ id, name, arguments, allow }`, but the route body type and code read `body.confirm_call.call` (a nested `ToolCall`). Fixed by (1) wrapping `confirm_call` to the declared `{ call: { id, name, arguments }, allow }` shape in `streamAssistantReply` (apps/web/src/ai/GroqStreamingProvider.ts), and (2) hardening the route to return 400 `"confirm_call requires a valid call"` instead of crashing when `call` is missing/invalid (apps/web/src/app/api/llm/route.ts). Verified live on groq: correct `{ call, allow:false }` → 200 (assistant replies "skipped"); flat legacy shape → 400 (not 500). typecheck/lint/build/test green.

Mood tracking (Mia suggestion 2026-09-05, typecheck/lint + mood probe green): per-user mood log so Mia records "kamu sedang butuh apa" and can tailor support. New `apps/web/src/lib/mood.ts` — fixed vocabulary `MOOD_VALUES` (great/good/okay/meh/stressed/anxious/sad/tired/angry) with IN-EN normalization ("capek"→tired, "bahagia"→great, unknown→meh), `addMood(mood, rawUser, note?)`/`readMoods`/`listMoods(since?)`/`moodTrend` at `.data/users/<user>/moods.json` (same atomic-write + sanitized-user pattern as tasks; capped `MAX_MOODS=200`, corrupt→[]). New tools `mood_log` (risk **read**, auto — takes mood + optional note) and `mood_recent` (risk **read**, auto — returns `moodTrend` + `listMoods` historial; used for "gimana mood-ku belakangan ini"). Deterministic capture `apps/web/src/lib/moodIntent.ts`: `detectMoodIntent(text)` regex-matches state-of-mind statements (stres/capek/sedih/cemas/marah/...), `logDetectedMood` writes via `addMood`; wired into **both** agent paths (`agent.ts logMoodFromMessages` after `scheduleReminderFromIntent` in the opencode branch and after the reminder block in the groq/9router branch) so moods log even when the model never emits a tool call. SYSTEM_PROMPT lists the tools + usage rule; OPENCODE_SYSTEM_PROMPT tells it to acknowledge mood (system records it automatically). Verified probe: normalize INT→EN, persistence round-trip, neutral queries → null, `logDetectedMood` writes; verify.ts + typecheck + lint green. **Live-verified on 9router (2026-09-05):** "aku lagi stres banget kerjaan numpuk" → entry `stressed` ditulis; "gimana mood-ku belakangan ini?" → model memanggil `mood_recent` & melaporkan akurat. BUG FIXED (double-log): `mood_log` tool (model) dan `logDetectedMood` (deterministik setelah turn) sama-sama menulis duplikat → `addMood` kini dedupe (entry bermood sama dalam `DEDUPE_WINDOW_MS`=10s dicollapse ke entry pertama; probe: tool+detect → 1 row, mood berbeda tetap append). Residual: trend adalah lightweight count heuristics (no ML); detection keyword-based — "stres" di luar konteks state bisa false-positive (probe netral → null); model 9router kadang menambah-nambahi count ("2 entri") padahal store 1 entri — data layer akurat, hanya embellishment LLM; no UI untuk mood history (ask via chat `mood_recent`).

**Spotify integration (2026-09-05, typecheck/lint + spotify probe + verify green):** lets Mia control the owner's playback via the Spotify Web API. `apps/web/src/lib/spotify.ts` — OAuth Authorization Code flow: `spotifyAuthUrl(user)` builds the authorize URL (SANITIZED user carried as `state`), `exchangeSpotifyCode` trades the code for a per-user refresh token stored at `.data/users/<user>/spotify.json` (atomic write; also `readSpotifyToken`/`clearSpotifyToken`), and every API call (`spotifyRequest`) auto-refreshes the short-lived access token (refresh on expiry or 401). Envs server-side only: `SPOTIFY_CLIENT_ID`/`SPOTIFY_CLIENT_SECRET` (+ `SPOTIFY_REDIRECT_URI`, default `http://localhost:3000/api/spotify/callback`) — must match the redirect registered in the Spotify dashboard exactly. Routes: `GET /api/spotify/auth?user=` (302→Spotify authorize; 500 when not configured) + `GET /api/spotify/callback?code=&state=` (exchange → redirect `/?spotify=connected|denied|error`). Tools (registry, `tools.ts`): `spotify_link` (read, returns connect URL or "sudah terhubung"), `spotify_status`/`spotify_search`/`spotify_devices` (read, auto-run), `spotify_play`/`spotify_pause`/`spotify_next`/`spotify_previous`/`spotify_volume` (write, FR-014 confirm). Shared `spotifyToolError` maps `spotify_not_connected`→ auth link, `spotify_no_active_device`→ "buka Spotify dulu" (player endpoints 404 with no active device). IMPORTANT: **playback-control endpoints require a Spotify Premium account** (status/search/devices work on free). SYSTEM_PROMPT tool list + usage + no-wait list updated; `verify.ts` checks registrations + not-connected path; spotify probe verified auth-URL building, token store, and not-connected messages. Residual: OAuth needs the owner to open `spotifyAuthUrl` once in a browser (no in-app popup); `spotify_link` in a voice turn returns a URL (not spoken well — text channels best); no device selection before play (uses active device).

Multi-session / resumable history (Mia fitur) DONE (typecheck/lint/build/test green, both verify proofs pass): builds on FR-015 to give each user multiple named, resumable conversations stored server-side (consistent with notes/reminders/persona isolation). New `apps/web/src/lib/sessions.ts` persists per-user sessions at `.data/users/<user>/sessions/<id>.json` + a lightweight `index.json` (SessionMeta: id/title/updatedAt/turns) capped at MAX_SESSIONS=12 (newest first); `upsertSession` (atomic index rewrite), `listSessions`, `loadSession` (full conversation), `deleteSession`. New `apps/web/src/app/api/sessions/route.ts`: `GET ?user=` (list), `GET ?user=&id=` (one, 404 if missing), `POST {user, session_id?, conversation, title?}` (upsert → {id}), `DELETE {user, session_id}`. Client: `useVoice` loads the most recent session on mount (restores via `ConversationManager.restoreHistory`), exposes `sessions`/`currentSessionId`/`switchSession`/`newSession`/`deleteSession`, and debounced-autosaves (1.5s after a turn) the active conversation; `AIChatCard` renders a conversations `<select>` + "+ New" + delete (Trash) strip under the orb. `restoreHistory([])` doubles as a clear for "new session". Verified end-to-end via server: POST create → GET list → GET one → DELETE all work and keep the index correct.

UI polish + mic-stop fix (2026-09-02, session DONE): AIChatCard redesigned — soft aurora border + ambient cyan glow (`animate-glow-pulse`) + `glass-strong` interior + top sheen; gradient header title; state pill under the orb ("Tap mic to start" / pulsing "Listening…"); hero circular gradient mic button placed in the footer below a `glass` pill composer with gradient send; messages get a gradient-cyan AI avatar dot, gradient user bubble, and an empty-state icon. BUG FIXED (mic couldn't stop): `toggleMic` decided on `capture.isActive()` (stream !== null) while the button/micActive derived from `micState.status` — the two could disagree, so clicking the "active" button sometimes re-STARTED instead of stopping. Fixed by making `toggleMic` use the same `micState.status === "active"` source as the button (single source of truth). Also hardened `AudioCapture.stop()` to async: now `await`s `stopRecording()` (so an in-flight MediaRecorder is stopped and its onstop resolved) before ending the stream tracks — awaiting stops the mic reliably even mid-recording. `useVoice.stop()`/`toggleMic()` now `await capture.stop()`. typecheck/lint/build/test green (vitest 9). Note: orb (`enableVoiceControl`) and PromptInput dictation open separate grab streams; only the main capture is driven by toggleMic.

OpenCode native streaming DONE (2026-09-02, typecheck/lint/build/test + both verify proofs green): the `opencode` provider now talks to the user's local `opencode serve` (default `http://127.0.0.1:4096`, set `OPENCODE_SERVER_BASE`) via its **proprietary session/agent API**, not OpenAI-compatible HTTP — fixing the old circular self-proxy (`OPENCODE_LLM_BASE=http://localhost:20128/v1/chat/completions` pointed at the voice app's own next-server, empty `open-code` replies). New `apps/web/src/lib/opencode.ts`: `runOpenCodeTurn({systemPrompt, messages, onDelta, signal})` → POST `/session`, open `GET /event` SSE, POST `/session/:id/prompt_async` (system-prompt text part + one text part per conversation message; optional `OPENCODE_PROVIDER`/`OPENCODE_MODEL`), then reads SSE events. It streams **only the assistant reply text**: captures `assistantMessageID` from `message.updated` (role=assistant) and diffs `message.part.updated` text parts whose `messageID` matches (the prompt's own text parts arrive as text parts too and would otherwise be emitted as the answer). Robustness backstops: (1) hard `OPENCODE_TURN_TIMEOUT_MS` (default 60000) watchdog aborts the session if the agent stalls; (2) any `permission.updated` is auto-**rejected** via `POST /session/:id/permissions/:id` so a tool-permission prompt can never hang the turn (no confirmation UI in a voice turn; the assistant's own tools already go through FR-014); (3) temp session is `DELETE`d in `finally` (no session bloat). `/api/llm/route.ts` short-circuits `providerId === "opencode"` **before** `resolveProvider`/model validation (bypassing the old proxy env) with its own `OPENCODE_SYSTEM_PROMPT` + `buildOpenCodeSystemPrompt(rawUser)` (still loads persona) and streams deltas through the same `text/plain` emitter — client (`GroqStreamingProvider`/`useVoice`/`ConversationManager`/`AIProvider`) is **completely unchanged**. System prompt firmly forbids OpenCode executing its own tools (write/bash would stall or surprise mid-turn); persona `<persona>` live-updates still apply. `providers.ts` opencode spec updated (description "Native local opencode agent", model label "Auto (server default)"); `.env.example` updated (old `OPENCODE_LLM_*` removed). Verified live: single-turn factual → 200 streamed; prior-context → 200 "Naufal" (previously HUNG on a tool permission); mock provider regression still 200. Note OpenCode server must be running (`nohup opencode serve --port 4096 &`), unauthenticated local REST/SSE only (no WS endpoint exists). Known residual: OpenCode hands you its local model's answers only — its agent tool power is intentionally disabled for voice-turn safety.

Mobile app-like UI fix (2026-09-03, typecheck/lint/build/test green): the SPA looked cramped on phones because it had NO viewport meta and used absolute positioned header/card/footer that could overlap, no safe-area handling, no iOS zoom prevention, and a stray second (default) viewport that overrode manual tags. Fixed: `apps/web/src/app/layout.tsx` now `export const viewport` with `width=device-width, maximum-scale=1, user-scalable=no, viewport-fit=cover, themeColor:#000000` (+ apple-mobile-web-app-capable/status-bar-style/title metas) → exactly ONE viewport in the HTML; `apps/web/src/app/globals.css` adds `@supports (-webkit-touch-callout:none){inputs{font-size:16px!important}}` (stop iOS auto-zoom), `.safe-top`/`.safe-bottom` via `env(safe-area-inset-*)`, `html/body{overflow:hidden;height:100%}` (no scroll bounce/double scrollbar), `touch-action:manipulation` + tap-highlight transparent, thin mobile scrollbar; `apps/web/src/app/page.tsx` refactored from absolute → flex column (`h-dvh` main, header `shrink-0`, chat card `flex-1 min-h-0`, status line `shrink-0`) so the card fills the whole viewport like a chat app; `ai-chat.tsx` message container got `min-h-0` so its internal scroll works inside flex.

Automatic persona memory (Mia-style) DONE (2026-09-03, typecheck/lint/build/test green): the old design made the model *voluntarily* append a hidden `<persona>` tag to persist facts, which models (esp. local OpenCode) often skip — so "remember to call me Mas Naufal" wasn't reliably saved. New deterministic capture: `apps/web/src/lib/autoMemory.ts` exports `captureFactsFromTurn({providerId,url,apiKey,defaultModel,persona,messages,rawUser})` which after each turn runs a dedicated NON-streaming extraction pass (OpenAI-compatible `extractFactsOpenAi` or OpenCode `extractFactsOpenCode` via `runOpenCodeTurn`) asking for NEW stable facts/style in `<persona>user.a=X;soul.b=Y</persona>` form, then persists each via existing `upsertPersonaFact` (dedups in place under `## Facts`/`## Style`). `parsePersonaTag(text)` is exported + unit-testable. `/api/llm/route.ts` calls it `void captureFactsFromTurn(...)` (fire-and-forget, never awaited → zero TTFT cost) in BOTH the opencode branch (persona=opencodeSystemPrompt) and the groq/9router branch (before returning the stream). Because capture is now deterministic, `SYSTEM_PROMPT` + `OPENCODE_SYSTEM_PROMPT` were changed to tell the model to NOT append any `<persona>` tag (fixes the tag leaking into the streamed answer, which would otherwise be TTS'd). `GroqStreamingProvider` strip-tag still present as a harmless fallback. Persona USER.md/SOUL.md normalized to the `## Facts`/`## Style` section format so upsert dedupe sees existing keys. Verified live on opencode: "aku suka kopi susu" → `- favorite_drink: kopi susu gula aren` auto-appeared; "hobinya badminton" → `- hobby: main badminton` auto-appeared; answer stayed tag-free and it addressed "Mas Naufal". Residual: fire-and-forget capture runs in-process — on serverless/edge it could be torn down after the response; fine for the VPS/`next start` path. Also note persona auto-capture only triggers when a user message has content (skips hydration/restart turns).

Persona emoji/identity + TTS emoji-strip DONE (2026-09-03, typecheck/lint/build/test green): `IDENTITY.md` (naufal + template) gained an Mia-style header — `Name: Mia`, `Creature`, `Vibe: Ramah, sopan, rajin, menyenangkan`, `Emoji: 🌸` — and `SOUL.md` says 🌸 is a signature emoji allowed in text replies but never spoken aloud. User's base64 avatar decoded to a valid WebP at `apps/web/public/mia-avatar.webp` (served at `/mia-avatar.webp`) — deliberately kept OUT of the persona so it isn't sent to the LLM every turn; use it as a UI asset. `GroqStreamingProvider.speak()` now strips emoji/pictographs before `/api/tts` via module-scope `stripEmojiForSpeech` (EMOJI_OR_PICTO_RE: 1F000–1FAFF, 2600–27BF, FE0F/200D joiner, regional flags) and skips audio when a sentence was emoji-only, so the text UI keeps 🌸 but TTS never vocalizes it. Verified live on opencode: Mia answers "Emoji aku bunga sakura... 🌸" (recognizes its emoji + vibe). Residual: restriction is prompt-driven so a very disobedient model could still emit emoji in a voice turn, but the TTS strip is a hard guarantee.

Persona emoji enforcement (2026-09-03, typecheck/lint/build/test green): follow-up to the emoji-strip work. Mia sometimes answered "emoji apa? / emoji kamu mana?" inconsistently ("nggak pakai emoji", "emoji off biar ringkas") even though 🌸 was in IDENTITY.md. Root cause was twofold: (1) an internal contradiction — IDENTITY said "short, plain sentences with no abbreviations" which the model read as "no emoji"; (2) history chat saying "emoji off" overrode the persona on later turns. Fixes: IDENTITY.md now adds "(In text replies I may use my 🌸 emoji — it's my signature, see SOUL.)" to end the contradiction; and both `OPENCODE_SYSTEM_PROMPT` + `SYSTEM_PROMPT` in `/api/llm/route.ts` now open with a terse, always-present line "You are Mia, a woman (she/her) ... your signature emoji is 🌸 (bunga sakura), use and answer it when asked." so identity lives in the strong server-side prompt (not ignorable persona blobs). Verified live with a contradicting history: ask after a prior "emoji off" turn → "Emoji bunga sakura itu bagian dari diriku, jadi aku tetap pakai itu ya." typecheck/lint/build/test green. Residual: opencode model still occasionally returns an empty/brief variant — identity robustness is now prompt-backed, not fully deterministic.

Reminder intent on the opencode path DONE (2026-09-03, typecheck/lint/build/test green): OpenCode's agent loop can't call server-side tools, so "bangunin aku jam 9 pagi" previously only got a verbal promise with nothing scheduled. New `apps/web/src/lib/reminderIntent.ts`: `parseClockTime` (Indonesian+English: "jam 9 pagi", "9 malam", "3 sore", "12 siang", "9:45", "9am/pm", "besok" → next-or-today via `nextOccurrence`), `detectReminderIntent(text)` (INTENT_RE: bangunin/banguni/bangunkan/bangun/ingetkan/ingatkan/ingetin/ingat/remind/reminder/set an alarm/alarm/wake me up/jangan lupa/kasih tahu/beritahu/bangun aku) → `{text, atMs}` or null. `/api/llm/route.ts` opencode branch (after captureFactsFromTurn): finds last user message, on intent match calls `addReminder(intent.text, intent.atMs, body.user)` and appends a spoken confirmation suffix ` (Sudah kusetel reminder pukul HH:MM, nanti kubangunkan.)` unless the model's own reply already mentions remind/ingat/alarm/bangun. OPENCODE_SYSTEM_PROMPT updated to tell the model it CAN remind/wake the user (the system does the scheduling). Verified: parser test 9/9 (incl. negatives "apa kabar?", "hitung 2+2"); live "bisa bangunin aku jam 7 pagi?" → opencode answered "Siap Mas Naufal, udah aku set ya..." HTTP 200 AND a `fired:false` reminder row at 07:00 landed in `.data/users/naufal/reminders.json`. Residual: parser is best-effort heuristic (relative times like "5 menit lagi" not parsed); reminder is scheduled server-side but delivery still depends on an open SSE stream existing at due time.

Fase 2 — Telegram Bot DONE (2026-09-03, typecheck/lint/build green, live-verified via @inimiaku_bot): Mia is now reachable from Telegram, long-polling **in-process with Next** (single process, no separate service). Architecture per PRD v2.0 Channel Adapter: the core turn logic was extracted from `/api/llm` into `apps/web/src/lib/agent.ts` (`runAssistantTurn({messages,provider,model,user,confirm_call})` → `{text,needsConfirmation}`; providers resolution, mock/opencode/groq/9router branches, model validation, tool confirmation, auto-memory, reminder intent, `CONFIRM_FRAME_PREFIX`, `MAX_TOOL_ROUNDS`). Both the web route and the bot import it directly (no HTTP-to-self). `/api/llm/route.ts` is now a thin wrapper mapping errors → status (invalid model 400, not-configured 500, LLM error 502) + streaming. New `apps/web/src/channels/telegram.ts` (grammY): starts via Next instrumentation — `instrumentation.ts` dispatcher (`NEXT_RUNTIME==="nodejs"` → `await import("./instrumentation-node")`) → `instrumentation-node.ts` calls `startTelegramBot()` **fire-and-forget** (`void bot.start(...).catch(...)` — NEVER `await bot.start()` inside `register`, it never resolves and blocks server readiness so GET / hangs; compact `notFound()` pattern avoided). Bot features: chat-history per chat, commands `/start /help /reset /provider /model`, **allow-list owner** via `TELEGRAM_ALLOWED_USERNAME` (default `naufalazhar65`), risky-tool inline confirm ("Balas 'ya'/'tidak'"), reminder push to owner chat via `subscribeReminders`. Builder gotchas: Next 15 ignores the `.node.ts` naming convention — use ONE `instrumentation.ts` with the edge/node dispatcher; `bot.start()` fire-and-forget to avoid blocking readiness. Env (`.env.local`, git-ignored): `TELEGRAM_BOT_TOKEN`, `TELEGRAM_ALLOWED_USERNAME`, `TELEGRAM_PROVIDER` (default `groq`; the bot can't read the browser's localStorage provider), `TELEGRAM_USER` (sanitized user key for notes/reminders/persona), optional `TELEGRAM_CHAT_ID`. BUG FIXED (reminder scheduled in the past): Groq's model emitted `when:"2026-05-28T13:00"` for "jam 1 siang" (5 months stale — the model has no live clock). Fixes: (1) `buildSystemPrompt` (and the refactored `currentTimeLine()` helper now shared with the OpenCode prompt) injects the current local date+time and instructs the model that a bare clock time = TODAY (or TOMORROW if passed), never invent a date; (2) safety net in `scheduleReminder` (`tools.ts`): any `Date.parse` result in the past rebases to `nextOccurrence(hour,minute)` so a relative reminder never fires immediately. Verified: re-ask → `when:"2026-09-03T13:00:00+07:00"` (correct), reminder row `fired:false` landed on disk. Verified end-to-end: hello/persona ("Naufal"), `remind_me` confirm "ya"/"tidak", `/help`, reminder delivery. Residual: bot is long-poll + in-process (single instance; multi-node needs external queue); non-owner chats are ignored; reminders only deliver if the bot process is up at due time.

OPS GOTCHA — "AI tidak merespons" / long hang on opencode (2026-09-03): the `opencode` provider's LLM backend is opencode serve (`~/.config/opencode/opencode.json` models `9router/ngoding` → baseURL `http://127.0.0.1:20128/v1`). When that local 9router proxy is DOWN, opencode serve loops `AI_APICallError: Cannot connect to API` retries for a full turn — `/api/llm` still returns HTTP 200 but only after 30–400s, so the browser/curl (short timeout) aborts first and you see `net::ERR_EMPTY_RESPONSE` / `HTTP:000` with an empty reply = "AI doesn't respond". This is NOT a code bug. Fix: ensure the 9router proxy is running (`lsof -ti tcp:20128` non-empty) and `opencode serve --port 4096` is up; both must be UP or text/voice hang. Also: there can be TWO `opencode serve` processes colliding on global state — kill all (`pkill -f "opencode serve"`) and start exactly one (`nohup opencode serve --port 4096 --print-logs > /tmp/opencode-serve.log 2>&1 &`). Groq text bypasses this entirely (fast, ~600ms); switch the app provider to Groq if you want zero dependency on the local proxy. Quick triage: `lsof -ti tcp:20128`, `lsof -ti tcp:4096`, `lsof -ti tcp:3000` — all three should be UP.

## Session 2026-09-07 — brain upgrade, Codebase QA, Weekly Insight, Mac control & monitors (all committed)

**OpenCode Go provider DONE (typecheck/lint/verify/test green, live-verified GLM 5.2):** new provider `opencodego` (OpenCode Zen "Go" subscription, $10/mo) — OpenAI-compatible gateway `https://opencode.ai/zen/go/v1/chat/completions`, Bearer auth. Key from env `OPENCODEGO_API_KEY` OR the opencode CLI login (`~/.local/share/opencode/auth.json` slot `opencode-go`) via NEW `serverKeys.ts ensureOpenCodeGoKey()` (server-only; providers.ts must stay client-safe — Settings UI imports its metadata, node built-ins there break the browser bundle). Wired into agent.ts + summarize.ts + consolidate.ts. **Go gateway requirements:** every request carries a stable `x-opencode-session` header (derived per-user in `runAgent`) + client User-Agent `mia-assistant/1.0`; strict schema validation REJECTS extra fields on `tools[]` → `runOneCompletionOnce` now serializes standard tool fields only (Mia's `risk` marker is no longer sent to any LLM). Defaults: `OPENCODEGO_MODEL` (now `deepseek-v4.1-flash` on this machine; spec default `glm-5.2`); brain switched everywhere via `.env.local` (`DEFAULT_AI_PROVIDER`/`NEXT_PUBLIC_DEFAULT_AI_PROVIDER`/`TELEGRAM_PROVIDER`/`DISCORD_PROVIDER` = `opencodego`) — voice STT/TTS stays Groq. Live: persona answer 3.2s, calculator tool 5s correct.

**Telegram typing indicator DONE:** `telegram.ts withTyping()` re-sends `sendChatAction("typing")` every 4.5s (indicator lives ~5s) around BOTH the normal turn and the confirm continuation; stops ≤250ms after the reply. Note: some Telegram clients barely render bot typing status — a one-time interim "mikir dulu" message was tried and REMOVED per user preference. Empty-text fallbacks ("…") replaced with a warm "jawabannya kepotong — coba tanya lagi ya 🌸" on both channels.

**Quality fixes (mood/reminder/spotify, user-reported):** (1) 9router wrote tool calls as PROSE (`mood_log(...)`, `remind_me(...)`) that leaked into chat → `stripToolCallProse()` in agent.ts removes tool-call-looking lines (fence-aware, all providers) — **critical ordering: strip runs BEFORE the deterministic post-processors** (a prose `remind_me(...)` containing "remind" used to suppress the reminder confirm-suffix, then got stripped → bare "…" reply even though the reminder WAS scheduled). (2) Telegraphic mood/greeting replies ("Beb lelah. Hari berat.") → `ensureMoodReplyQuality()`: if the message is a mood/greeting turn and every sentence is ≤4 words, replace with a warm deterministic empathy line (rotated daily; mood variants per mood label). Lesson: prompt BAD-examples get imitated by small models — rules must be described, never quoted; telegraphic junk was also scrubbed from daily memory (self-priming via RAG recall). (3) Indonesian clock: "jam 1 siang" = 13:00 (was 12:00); bare clocks ("jam 8 pas") resolve to the NEAREST FUTURE interpretation (`nearestOccurrence`) so an evening ask never lands on tomorrow morning. (4) Spotify double-play: macOS `open spotify:track:<uri>` deeplink ALREADY starts playback — the polling loop no longer fires `start()` again (pre-flight `currentPlayer` check); playback claims are verified via `verifyPlayback` (GET /me/player is_playing) before saying "sudah diputar" (honesty guard); "(Sudah kuputar: …)" suffix only on verified success.

**Codebase QA DONE (new feature):** `codebaseIndex.ts` indexes repo + allowed workspaces (multi-root sandbox) → ~80-line chunks → BM25 (shares `bm25Scores`/`tokenize` with rag.ts so ranking can't drift; embeddings skipped — identifier exact-match is what code search needs). Bounded (15s budget, 4000 files/12000 chunks) with file_read-style deny rules (.env/secrets/node_modules/binaries — tested). Persisted atomically to `.data/codebase-index.json` + globalThis cache; auto-rebuild when >24h or roots change. Tools `codebase_search` + `codebase_refresh` (both read, auto-run). Fix during live-test: the model chained 4-5 tool rounds (search→read→exec→search) and died on "too many tool rounds" → `MAX_TOOL_ROUNDS` 3→5 + 1200-char snippets + tool description ordering ("answer from snippets; at most one follow-up read"). `exec_write` now allowlists `npm test`/`npm run <script>` (150s timeout — unit suites die on the old 10s; `npm notice` noise filtered from stderr) with FR-014 confirm; read-exec deny messages point the model at exec_write. Index lives in `.data` (gitignored); verified: 194 files → 491 chunks; live Q&A returns accurate file:line refs.

**Weekly Insight DONE (new feature):** `weeklyInsight.ts` — 7-day digest (moods with Indonesian labels + empathetic line by negative/positive ratio, task status incl. overdue, recurring conversation themes) built from existing stores; deterministic templates with day-seeded openers/closers (scheduled pushes must never depend on an LLM). **Theme counting is by DISTINCT DAYS a token appears (≥2 days), not raw frequency** — a one-evening game burst (hundreds of "lagu" lines) otherwise dominates; stopword list covers small-talk filler. Auto-push once a week (env `WEEKLY_INSIGHT_HOUR` default 20, `WEEKLY_INSIGHT_DAY` default Sunday, 0=off, Asia/Jakarta) via pushToOwner + tool `weekly_insight` on demand. Dedup persists to `.data/weekly-insight-state.json` (atomic) — recap's restart double-push bug, prevented from day one. Junk-filtered ([persona]/automation/timestamps); silent when a user has no data.

**mac_open (visible browser) DONE:** `browser_*` tools run Playwright HEADLESS (invisible by design) — users expect "buka youtube dong" to open a REAL window. New `mac_open` tool (read, auto-run): macOS LaunchServices `open <url>`, http(s)-only guard (file:// rejected), 5s timeout. SYSTEM_PROMPT rule: user wants to SEE it → mac_open; Mia reads/automates → browser_* (invisible). Live: model even combined mac_open + a YouTube search URL so the visible window lands on results.

**Bluetooth control DONE:** `device_exec` (already FR-014 confirm) gained `blueutil -p [0|1]` (status/off/on) — strict arg validation (only those exact forms; injection/other values rejected), ENOENT mapped to a friendly "brew install blueutil", 0/1 output translated to "Bluetooth: OFF/ON". Requires `brew install blueutil` (2.13.0 installed on the owner's Mac). SYSTEM_PROMPT maps "matiin bluetooth" → `blueutil -p 0`. Live: confirm → real power off → natural reply.

**Mac health monitors DONE (battery/storage) + LATENT BUG FIXED:** `monitor.ts` gains kind `"device"` (subject battery|storage, percent threshold 1-100; battery alerts ≤, storage ≥ — inclusive on the boundary; merged on re-ask). Metrics read the local Mac: battery via `pmset -g batt` → ioreg fallback, storage via `df`. **BUG: `checkMonitorsAndAlert` was exported but NEVER CALLED anywhere** — crypto/web watchlist monitors could never alert since the feature shipped. Now wired in the heartbeat tick (alert pushed as `👁️ *Monitor*`; fires once per crossing, re-arms when back safe). **BUG VOLUME: `df /` on macOS reads the sealed SYSTEM snapshot (always ~39%) — the real data volume is `/System/Volumes/Data` (91%)**; `readMacStoragePct` reads the Data volume, and read-exec REMAPS any `df ... /` argument to the Data volume on darwin so model-generated `df -h /` answers honestly; SYSTEM_PROMPT example updated. `df` added to the read-exec allowlist. SYSTEM_PROMPT documents the monitoring ask ("say you'll watch it warmly — the system schedules it; check current value via device_battery / df").

**Deterministic mood capture & telegraphic guard (2026-09-07, agentic quality):** `ensureMoodReplyQuality` covers mood AND greeting turns (detectMoodIntent / greeting regex incl. "sayang", excluding clock/reminder asks), rewrite only when the reply is telegraphic (all sentences ≤4 words), variants rotate by day. Live-verified on GLM 5.2: "capek banget hari ini" → warm full-sentence empathy.

**Gotchas learned (2026-09-07):**
- **tsx probes do NOT load `.env.local`** (only `next dev` does) — probes needing provider env must parse `.env.local` manually into `process.env` before importing lib modules.
- **Deterministic post-processing ORDER matters**: strip prose before suffix decisions; a tool-call-looking model reply both matches "remind" (suppressing the suffix) and gets stripped → empty reply.
- **Never quote BAD examples in prompts** — small models imitate them verbatim (telegraphic mood replies appeared right after a GOOD/BAD prompt edit).
- **Self-priming via RAG**: bad replies stored in daily memory get recalled into later prompts — scrub junk lines from the record when fixing output quality.
- **macOS storage**: `df /` = sealed system snapshot (~40% always); the honest number is `/System/Volumes/Data`.
- **Strict gateways** (OpenCode Go) reject non-standard fields anywhere in the request (tools[].risk) — serialize standard OpenAI schema only.
- **Telegram typing** is best-effort UI: some clients don't render bot typing; keep it (cheap) but don't rely on it as the only waiting signal.

## Session 2026-09-17 (format push reminder) — kalimat penutup nyempil

Owner melaporkan push reminder terasa aneh:
`🌸 Mia — Mas Naufal, udah makan siang belum? Jangan skip ya 😄 · pukul 12:12` lalu baris kedua `Pelan-pelan aja, aku di sini`.

Akar masalahnya bukan satu kalimat, tapi **desain templating**: `reminderMessage` membungkus SEMUA teks reminder dengan template (`{text}`, `Beb, {text} 🌸`, `saatnya {text} 🌸`, `{text}, yuk.`), menambahkan TAILS acak, lalu membuang 🌸. Untuk kalimat utuh hasil model ini menghasilkan omong kosong (tercetak saat audit): `"saatnya Bangun tidur Mas Naufal!"`, `"…belum? Jangan skip ya 😄, yuk."`, `"Beb, Mas Naufal, udah makan siang belum?…"`, `"Beb, Isi perut dulu ya…"`.

Perbaikan (menyederhanakan, bukan menambah aturan):
- **Kalimat utuh dipakai APA ADANYA** (tanpa template, tanpa TAILS — satu baris).
- Template hanya untuk **teks pendek** (`isTerseReminder`: ≤2 kata tanpa tanda baca/emoji, mis. "makan"/"minum air"), dan selalu memilih varian yang menutup sendiri (`hasOwnCloser`).
- TAILS dihapus seluruhnya; 🌸 tetap satu (dari wrapper channel), jam tetap 24-jam WIB.
- Helper murni `hasOwnCloser`/`isTerseReminder` + tes (total `vitest` 27 tes) yang mengunci: keluaran satu baris, tanpa template untuk kalimat utuh, tanpa "Pelan-pelan/Semangat".

## Session 2026-09-17 (verbatim hijack) — balasan tertimpa daftar reminder

Owner mengirim "halo" tapi menerima balasan daftar reminder. Reproduksi menunjukan rantai sebenarnya: pada **"ingetin aku makan siang ya nanti jam 12"** model memanggil `reminders_list` (untuk konteks), dan **verbatim fast-path** (`VERBATIM_LIST`) mengganti SELURUH balasan Mia dengan output tool mentah → user tidak pernah dapat konfirmasi, dan intent `remind_me` yang belum tuntas "bocor" ke pesan berikutnya (muncul prompt konfirmasi lain di giliran "halo"). Penjadwalan deterministik sebenarnya tetap berjalan (suffix `(terjadwal)`), tapi balasannya menyesatkan.

Perbaikan: gate di `agent.ts` — fast-path hanya menimpa balasan bila **user memang meminta daftar itu**:
- `userAskedForList(tool, userText)` (pure, tested): tool daftar **personal** (`reminders_list`, `list_tasks`, `list_notes`, `calendar_list`, `plan_list`, `gmail_*`, `hotel_search`, `persona_show`, …) hanya menimpa bila pesan user memuat kata daftar/status (`LIST_ASK_RE`) dan bukan permintaan SET (`SET_VERB_RE`), kecuali ada permintaan eksplisit "apa aja/daftar/lihat/cek" ("ingetin aku, reminder apa aja yang aktif?" → tetap daftar).
- Tool task/security (recon, finding, poc, suite_hunt, …) tetap selalu menimpa — outputnya memang deliverable-nya, dan sapaan tidak mungkin memicunya.
Verifikasi: "ingetin aku makan siang jam 12" → balasan konfirmasi normal (tidak ada daftar), "halo" → sapaan, "reminder kamu apa aja?" → daftar verbatim (masih benar).

## Session 2026-09-17 (double-turn) — satu pesan diproses DUA kali

Owner menerima **dua prompt konfirmasi `remind_me`** untuk satu pesan "ingetin aku makan siang jam 12". Log menunjukkan pesan Discord yang sama di-handle dua kali (`msg … / turn start / turn done` dua kali) dan Telegram mencatat `409 Conflict: terminated by other getUpdates request` = **ada instance kedua hidup** saat itu (prosedur restart lama `pkill -f "next dev"` + `sleep 3` tidak menjamin anak `next-server` mati sebelum instance baru start).

Perbaikan (kelas bug: inbound message / start runner harus "sekali per PROSES"):
- **`lib/once.ts`** — primitif process-wide di `globalThis` (module-level `let` di-reset oleh HMR sehingga guard/start bisa lahir dua kali): `alreadyProcessed(kind, id)` (dedupe pesan masuk, ring 500 id) + `alreadyStarted(key)`/`resetStarted(key)`.
- **Discord**: `alreadyProcessed("discord", msg.id)` di awal handler (duplikat di-log `duplicate message ignored`, bukan diproses) + dedupe `interaction.id`.
- **Telegram**: satu middleware `bot.use(...)` mendedupe via `ctx.update.update_id` (menutup semua tipe pesan: text/voice/document/photo).
- **Guard start** bot + seluruh runner (heartbeat, briefing, weekly, recap, winddown, context, auto-updater, automation-runner) kini `alreadyStarted(...)` — HMR tidak bisa menambah timer/client kedua (dulu bisa → push dobel).
- **Prosedur restart** di AGENTS diperbaiki: kill **per port** + tunggu port bebas (lihat HABIT di atas), lalu verifikasi `grep -c "logged in as"` = 1 dan tidak ada 409.

Verifikasi setelah perbaikan: start log berisi tepat satu `logged in as`, satu `[telegram] starting`, **0** 409, dan setiap runner log `starting —` sekali. `vitest` +`once.test.ts` (dedupe window/cap + guard idempotence) → 24 tes.

## Session 2026-09-17 (live drill + anti-regresi) — bug dari giliran nyata

Owner menjalankan drill end-to-end (giliran nyata lewat `runAssistantTurn` seperti adapter Discord). Dua bug baru ketemu **dari drill itu**, bukan dari audit statis:

1. **Prompt automation yang memuat "SKIP" benar-benar men-skip lagu.** Prompt automation cuaca berakhiran "balas tepat: SKIP"; `NEXT_RE` di `spotifyIntent.ts` memuat kata `skip`, jadi `detectSpotifyControl` membacanya sebagai perintah next dan jalur deterministik memanggil `spotifyNext` → **lagu owner keskip**. Tiga lapis perbaikan:
   - `isPlaybackCommand()` (pure, tested): teks hanya dianggap perintah bila mengarahkan kata perintah di AWAL (setelah kata sopan "tolong/coba/dong") atau menyebut objek musik ("lagu/musik/suara/volume") — prosa panjang tidak lagi mengontrol playback.
   - Giliran **headless** (`autoDenyRisky`, yaitu automation/webhook) tidak menjalankan fallback Spotify deterministik sama sekali.
   - Giliran headless **menolak tool `read` yang punya efek nyata** (`HEADLESS_SIDE_EFFECT_TOOLS`: 8 tool Spotify + `mac_open` + `send_channel`) seperti tool risky, agar halusinasi model dari konteks tak menyentuh dunia nyata.
   Hasil drill: balasan automation tepat `SKIP`, tidak ada skip lagu, tidak ada push.
2. **`poc_verify` hanya menilai BODY**, sehingga temuan cookie (bukti di header `Set-Cookie`) selalu tampak "tidak stabil" walau deterministik. Kini: `expect_header`, `expect_header_absent`, dan `expect_cookie`+`expect_cookie_missing` (helper `cookieMissingFlags` — memeriksa SATU baris cookie, jadi cookie saudara yang punya HttpOnly tidak menutupi temuan), serta verdikt dinilai dari **assertion** (`3/3 PASS`) bukan kesamaan byte — token dinamis (session id/tanggal) membuat byte body/header wajar berbeda. Drill: temuan cookie pulsepoint kini **✅ PoC STABIL & terkonfirmasi 3/3**.

Drill lain yang lolos: `spotify_play` favorit **tepat 1×** (tidak dobel), "stop kalau lagunya selesai" → sleep timer (bukan pause), cuaca "Lake Home" → 32°C/44% (lokasi benar), `mood_recent` normal, dan `suite_hunt` pada `checkout.webmd.com` menghasilkan 3 lead + tercatat ke `hunt_log`.

**Anti-regresi:** `vitest` kini juga menjalankan `apps/web/src/**/*.test.ts` dengan `turnRouting.test.ts` (11 tes) yang mengunci kelas bug ini di level keputusan: routing Spotify turn-level (`planSpotifyTurn` — satu pemilik keputusan, diekstrak dari blok post-processing), dedupe aksi yang sudah dijalankan model, `isPlaybackCommand`, `moodTone`, `isFillerLine`, `isSilentAutomationReply`, `clockLabel`/`wibDay`/`wibDayIndex`/`wibDailyNext`, dan `parseLatLonAnywhere`. Total 21 tes (`npm test`).

## Session 2026-09-17 (audit teliti #2) — waktu/zona, idempotensi, dan cakupan alias

Audit menyeluruh ketiga menemukan 6 bug lagi (semua sudah diperbaiki + diuji):

1. **"Yesterday" berbasis zona SERVER.** `proactive.yesterdayDayStr()` dan alias `"yesterday"` di `dailyMemory.ts` memakai `setDate(getDate()-1)` (waktu server) lalu diformat WIB → di host non-WIB membaca hari yang salah di sekitar tengah malam. Kini `wibDayBefore()` (kurangi 24 jam — Jakarta tanpa DST — lalu format WIB).
2. **Lima (lalu enam) salinan formatter hari WIB** (`proactive`, `recap`, `briefing`, `weeklyInsight`, `windDown`, `habits`/`heartbeat` inline). Semua kini memakai **`lib/time.ts`** (`wibDay`, `wibDayBefore`, `wibDayIndex`).
3. **Jadwal harian automation memakai jam server.** `nextRunAt` daily membangun `new Date(y,m,d,hour,minute)` (zona server) → "setiap hari jam 7" bisa jalan 07:00 waktu server. Kini `wibDailyNext(hour,minute,after)` (dipin Asia/Jakarta, dihitung dari offset tetap +7).
4. **Tanggal UTC di 6 tempat**: `holiday.ts` (`toISOString` → hari libur tampak "sudah lewat" 00:00–07:00 WIB), `summarizePro` streak, label mood di `rag`, `mala` (ramalan ganti jam 07:00 WIB, bukan tengah malam), `auditLog`/`appLogger`/`learnings`/`clawicMemory`, dan label `[superseded]` persona → semua kini `wibDay()`. `dayRotated` memakai `wibDayIndex()` supaya rotasi harian berganti di tengah malam WIB.
5. **`startAutomationRunner()` tidak benar-benar idempoten** (komentarnya klaim idempoten, tapi tanpa guard) — panggilan kedua menambah listener kedua → setiap automation due dipush DUA kali. Kini pakai flag `runnerStarted`.
6. **Cakupan alias `home` terlalu luas**: `homeCoordsFor` mencocokkan SEMUA kata label, jadi permintaan cuaca "Tangerang"/"Serpong" ikut dijawab koordinat rumah. Kini hanya segmen **sebelum koma** (nama panggilan, mis. "Lake Home") yang dianggap alias; alamat setelahnya tidak.

Plus dua perbaikan ketahanan: `recap` menyebut mood netral dengan jujur (bukan "mood belum ke-log"), dan `reminderMessage` tidak lagi mungkin memilih dari pool kosong.

## Session 2026-09-17 (lanjutan) — audit kelas-bug yang sama

Setelah lima fix di atas, audit menyisir kelas bug yang sama di tempat lain dan menemukan tiga lagi:

1. **`waze_route` punya bug lokasi identik dengan weather.** `waze.ts` meng-geocode label mentah juga → "Lake Home" jadi tempat lain (jarak ngawur). Logika resolusi tempat kini SATU pemilik: **`lib/geo.ts`** (`parseLatLonAnywhere` + `homeCoordsFor` + `resolveExplicitCoords`), dipakai `weather.ts` DAN `waze.ts` (plugin meneruskan `ctx.rawUser`). Live: `waze_route("Lake Home","Monas, Jakarta")` → 44,7 km/86 mnt (masuk akal) dan `weather("Lake Home")` → 30°C/52%.
2. **Tie mood masih dianggap "positif" di dua tempat lain.** `weeklyInsight.ts` (`pos = moods.length - neg` — "meh" ikut dihitung positif, `pos >= neg` → "vibes kamu oke") dan `recap.ts` (`negative > positive ? … : positive > 0` → seri = "mood lumayan baik"). Keduanya kini memakai `moodTone()` + `NEGATIVE_MOODS`/`POSITIVE_MOODS` dari `mood.ts` (satu definisi; `recap.ts` juga memakai `isFillerLine`) dan punya cabang netral yang jujur.
3. **Jam 12-jam + timezone tersisa.** `agent.ts` (3 situs suffix reminder) memakai `toLocaleTimeString([])` → "pukul 06:00 AM" di chat; `spotify.ts` memakai id-ID tanpa pin zona. Kini semua lewat **`lib/time.ts` `clockLabel()`** (24 jam, dipin ke Asia/Jakarta — bukan zona server); `channels/replyChunk.ts` me-reexport sehingga adapter channel tetap satu definisi.

## Session 2026-09-17 — honesty/integrity fixes found from live pushes

Owner pasted a day of real pushes ("apa ada yang aneh?") — five root-caused fixes, all verified against real data:

1. **Wrong-location weather (highest impact).** `weather.ts` geocoded the raw label: `weather("Lake Home, Serpong, Tangerang Selatan (koordinat -6.378806,106.712563)")` → `Cannot get coords` (anchored parser) and bare `weather("Lake Home")` → wttr.in resolved a DIFFERENT place (25°C/80%) while the real Serpong was 31°C/52% — so 01:57/03:57 pushes reported another region's weather under the user's own nickname. Fix: `parseLatLonAnywhere` (bounded lookbehind/lookahead so "2026, 17" can't match, pure/tested) + `homeCoordsFor` (persona `home` label → `home_coords`, generic words like home/kota excluded so unrelated queries aren't hijacked) + when explicit coords are known, wttr is fetched BY COORDS (a name fetch could resolve elsewhere) while the human label stays the user's own. Prompt rule: on `Cannot get coords`, NEVER report another location. `getWeather(loc, rawUser)` now takes the user (tool plugin passes `ctx.rawUser`).
2. **Fabricated "kemarin kerasa capek dan berat".** `proactive.ts` fired its heavy openers whenever ANY negative mood existed, while `briefing.ts` required `neg > pos` → the same morning said both "kemarin lumayan cerah, seneng" and "kemarin kerasa capek dan berat". Fix: `moodTone()` in `mood.ts` (NEGATIVE_MOODS/POSITIVE_MOODS + tie = neutral — one definition, pure/tested) used by BOTH; proactive uses heavy openers only on `negative`, a neutral opener on a tie, and stays silent on `positive`/no data.
3. **Briefing tie** → neutral line ("Kemarin naik-turun ya beb") instead of the positive one.
4. **Overlapping weather automations** (owner data): the leftover 12h "cuaca Jakarta" automation was DISABLED and the home one moved from every-2h to every-6h. New runner contract: `isSilentAutomationReply()` (pure/tested) — a scheduled CHECK replies exactly `SKIP` when there is nothing to report and the runner then pushes nothing (previously it pushed "tidak ada tanda hujan" at 01:57 and 03:57).
5. **Filler leaked as a "topic" + reminder formatting.** `isFillerLine()` in `memoryNoise.ts` (pure/tested) drops pure small-talk ("alooo beb") from the briefing snippet, the proactive snippet and the weekly theme counter. `clockLabel()` in `replyChunk.ts` gives ONE 24-hour WIB "HH:MM" for both channels (was `toLocaleTimeString([])` → "· pukul 06:00 AM"), and `reminderMessage` no longer prepends "saatnya" to a greeting and strips 🌸 so the channel wrapper's single signature flower isn't duplicated.

## Session 2026-09-14 — Live-data tools (cinema / hotel / transport) + provider tool caps

**List output must be verbatim.** `hotel_search`, `cinema_showtimes`, `train_search`, `bus_search` were added to `VERBATIM_LIST` (`agent.ts`) so their formatted lists reach the user as-is — the model kept collapsing bulleted results into one paragraph. The hotel tool now returns ONLY `r.human` (no JSON leak). A confirmation continuation sets `collector.suppressVerbatim` so a follow-up `list_*` can't mask the real action outcome (e.g. `complete_task` failed → "Belum ada tugas" instead of the failure).

**`cinema_showtimes` (new, `cinema.ts`, keyless):** scrape jadwalnonton.com (aggregates XXI/CGV/Cinépolis). Modes: `cinema` (one theater's schedule), `film` (every cinema in a city showing it), else now-playing (+`genre`). City aliases map Tangsel/Serpong/BSD/Bintaro/… → `tangerang` (Tangsel cinemas are filed under Tangerang). `pageDate` strips tags (the date sits behind a `<span>`). Honest "tidak ada/tidak dikenali" on miss; bounded output; 15s timeout.

**`hotel_search` upgrade (`hotel.ts`):** `checkin`/`checkout` YYYY-MM-DD (validated; local-date default fixes a UTC/WIB off-by-one), `adults`/`rooms`, `sort`(price|rating|popularity), `minRating`, `stars`. Booking card price is the **N-night TOTAL** → shown as per-night + total, budget compared per-night. Fixed integer score parse ("Skor Skor 10 10" → "Skor 10"); extracts stars + distance. Unknown destination redirects to Booking home (NOT a bot-check) → honest "destinasi tidak dikenali"; "captcha" removed from the challenge markers. Dropped the 200k price floor; `budget` accepts number|string; 10-min result cache.

**`train_search` + `bus_search` (new, `transport.ts`, keyless):** train = Traveloka route page `__NEXT_DATA__` → `automationProps.schedules[]` (name, number, class, depart/arrive, duration, **fare**, stations); bus = busonlineticket.co.id route table (operator, first/last departure, trips, "dari" price). `slugify()` + aliases (Jogja→yogyakarta). Sources: `jadwalkereta.id`/`jadwalbis.com` were REJECTED (expired TLS cert). No date param yet; bus price is the cheapest ("dari"). Flights still need a keyed API (Amadeus free quota needs billing info; Duffel test = fake prices).

**Reminder honesty & wake sync (2026-09-16):** a *question* about reminders ("masih inget ga jadwal bangunin aku?") matched the set-intent regex and wrongly added/merged a reminder + appended a "(terjadwal)" suffix instead of answering — fixed with the pure `isReminderQuery` guard at the top of `scheduleReminderFromIntent`. Wake reminders and the persona `wake_up_time` fact are now kept in sync (`syncWakePersona`, called from `remind_me` and the deterministic scheduler; `cancel_reminder` drops the fact when the wake reminder is gone) so Mia never cites a stale hour. Automation runs no longer quote their own schedule text in the push.

**Spotify on capped providers (2026-09-16):** Spotify was absent on Groq/9router because `CORE_TOOL_NAMES` was already exactly 128 and `toolsForUrl` gives the `rest` fill `max(0, cap - core.length)` = 0 slots. The 12 Spotify tools were moved INTO CORE (right after the daily personal cluster, so they also fit 9router's 64 cap) and 12 report-time/niche tools were demoted (`plan_create`/`plan_add_step`/`plan_update_step`, `hardening_plan`/`hardening_pdf`, `verify_patch`, `lab_status`/`lab_start`/`lab_fetch`, `evidence_capture`, `recon_takeover`, `breach_check` — all still on `opencodego`, which has no cap). CORE stays exactly 128 and `verify.ts` now asserts `spotify_play`/`spotify_mode`/`spotify_queue`/`spotify_sleep_timer` survive BOTH caps.

**Spotify mode + queue (2026-09-16, tools 276):** `spotify_mode` (shuffle/repeat; `normalizeRepeat`/`truthyFlag` pure-tested; no args = honest status from `currentPlayer`, which now also reads `shuffle_state`/`repeat_state`) and `spotify_queue` (POST `/me/player/queue`, resolves 'lagu favoritku' from persona like `spotify_play`). Both risk `read` (immediate, no FR-014) matching every other Spotify control. Live-verified. Note: no scope change needed (all inside the 5 granted scopes); `spotify_like`/playlist-add/`spotify_top` would need a re-link.

**Spotify double-play fixed (2026-09-16):** a `spotify_play` the model emits ALREADY executes in the agent's tool loop (spotify tools are risk `read`), yet the deterministic post-processor ran `scheduleSpotifyFromIntent` too → the song restarted right after Mia's reply (live log showed two `play requested`, one with a mangled `'favorit ku'` query). Fix is turn-local dedupe: the loop records executed `spotify_*` names in `collector.spotifyCallsExecuted` (now a named `TurnCollector` interface, one declaration instead of two inline copies) and the post-processor only fills in what the turn did NOT already do — `scheduleSpotifyFromIntent` is skipped when `spotify_play` already ran, and the deterministic control is skipped when the model's identical action (`spotifyControlToolName`) already ran. Also new: `resolveFavoriteQuery`/`getPersonaFact` make "lagu favoritku" resolve from persona `preference.song` inside `spotify_play` (literal search of the phrase returned junk), and `confirmResume` reports the track actually playing ("Pemutaran dilanjutkan — yang jalan sekarang: X (artists)"). Verified live: same turn → exactly ONE play call, reply names the right song.

**Spotify honesty & after-track routing (2026-09-16, follow-up):** two live bugs. (1) "stop aja kalau lagunya udah selesai" was routed by the DETERMINISTIC control parser (`detectSpotifyControl` PAUSE_RE matched "stop") → an immediate pause cut the song; now `detectSpotifyAfterTrack` (pure, tested) runs first and `scheduleSpotifyControlFromIntent` arms `spotifySleepTimer(after_track)` instead. (2) `spotify_play` without a query RESUMES whatever was queued (it played "Film Favorit" while Mia claimed the requested M2M song): `confirmResume` now reports the real track from the player snapshot ("Pemutaran dilanjutkan — yang jalan sekarang: X (artists)") and the prompt forbids claiming a song unless the tool result names it (or call `spotify_status`).

**Spotify sleep timer (2026-09-16):** "stop lagunya kalau udah selesai" was answered with a `remind_me` (a reminder only notifies; it does not stop playback) at a guessed time. New `spotify_sleep_timer` (write/confirm, `spotify.ts`): `after_track=true` (pauses when the current track ends, computed from `duration_ms − progress_ms`), `minutes=N` (max 360) or `cancel=true`; no args = status. In-process timer (dropped on restart, said honestly); on fire it calls `spotifyPause` and pushes a ⏹️ note. Pure `sleepTimerPlan` is unit-tested. Prompt now routes those asks to this tool instead of `remind_me`.

**Secret-leak audit (2026-09-16):** three leak vectors closed. (1) The tool-confirmation prompt rendered every arg verbatim, so a `rapyd_request` secret_key / `jwt_attack` secret / `breach_check` password / `Authorization`+`Cookie` header showed up in Discord/Telegram — `redactArgsForDisplay` (in `args.ts`, recursive, key-based + pattern) masks those values while keeping the keys. (2) The audit log redacted only 5 tool names, so `http_request`/`jwt_attack`/`http_session`/`cdp_request` wrote JWTs and the auth0 id into `.data/audit/*.log` — logging now masks by KEY for every tool (`redactArgsForDisplay`) and `redactSecrets` also handles `rsk_`/`rak_` and JSON `"secretKey": "…"` values. (3) Outbound channel text masks the local home dir (`scrubHomePath`, `/Users/<name>/…` → `~/…`) at the `chunkText` boundary. Existing files were scrubbed (131 scanned → 7 cleaned, 0 residual). Intended credential stores (`http-sessions.json`, `spotify.json`) are deliberately untouched.

**Persona upgrade (2026-09-16):** `personaFacts.ts` (pure, tested) canonicalises fact keys (`favorite_food` == `preference.food` == `preference.favorite_food` → one key), resolves conflicts (latest value wins; the old value is recorded under `## Superseded` with a date — never injected into the prompt), **rejects secrets** (`looksLikeSecret`: JWT/Bearer/auth0|/AKIA/sk-/long hex/base64 — a pasted token is never stored), caps live facts (`MAX_FACTS=80`, core keys first via `sortFacts`) and splits facts from history (`splitFactFile`). `upsertPersonaFact` + `hygienizeUserFile` both use these helpers, so the two paths can't drift. New user-facing tools: `persona_show` (read/auto), `persona_set` (write/confirm, explicit 'ingat ini'), `persona_forget` (write/confirm). CORE stays 128 (`persona_*` in; `browser_eval`/`plan_list`/`nuclei_custom` demoted, still on opencodego). Existing user files were repaired once via `hygienizePersona` (name/city/favorite-* conflicts merged, e.g. `favorite_food`+`preference.food` → `preference.food: nasi goreng`, `bakso` → Superseded).

**Indonesian TTS (2026-09-16):** Groq Orpheus has no Indonesian model, so Indonesian was read with English pronunciation. `ttsLocal.ts` adds a fully LOCAL, keyless path: when `synthesizeSpeech` sees Indonesian text (`looksIndonesian`, pure-tested) on macOS it runs `say -v Damayanti` → `afconvert` → 16-bit PCM WAV. Falls through to the provider on any failure, so English/Arabic behaviour is unchanged. Env: `TTS_LOCAL_ID=0` disables, `TTS_ID_VOICE` changes the voice (default `Damayanti`). `say --data-format` is rejected for AIFF (`Opening output file failed: fmt?`) — let `say` choose and convert with afconvert.

**Provider tool caps (`agent.ts` `toolsForUrl`).** ~159 tools broke providers: Groq hard-rejects >128 tools (400) and the 9router/Gemini free tier 429s on a big payload. `toolsForUrl(url)` now caps per endpoint — Groq ≤128, 9router ≤64 — sending a **priority-ordered** subset (`CORE_TOOL_NAMES`, Set insertion order, NOT registry order) so daily tools (all 4 live-data tools, web_search, notes/tasks/reminders, …) always survive. Verified: `toolsForUrl` keeps `hotel_search`/`cinema_showtimes`/`train_search`/`bus_search` on BOTH caps (they were dropped on 9router until added to CORE). **2026-09-15:** the pentest action/report suite (`pentest_scan`, `sqlmap_scan`, `zap_scan`, `http_request`, `finding_*`, `report_*`, `cvss_score`, `dep_audit`, `verify_patch`, `hardening_*`, `engagement_*`, `lab_*`, `web_audit`, `domain_audit`) was missing on capped providers (advertised in the prompt but not sent) → added to CORE, so Groq now carries them (128). NOTE: 9router's ≤64 cap is a hard limit smaller than CORE (128), so it only carries the first 64 CORE entries (daily + recon/security reads) — the pentest write/report tools are unavailable there; use opencodego (no cap, all 276) or Groq for the full pentest workflow. `verify.ts` guards both.

**Freeride cross-endpoint guard (`agent.ts`).** When a provider was rate-limited, the freeride chain sent OpenRouter model IDs (`nvidia/*:free`) to the Groq/OpenCode endpoint → 404. The chain now applies ONLY to `openrouter.ai` / the local 9router proxy (`127.0.0.1:20128`).

**XML tool-markup leak.** `stripToolCallProse` now also removes leaked Anthropic/OpenAI-style XML tool markup (`<invoke>`, `<parameter>`, `<tool_call>`, `<tool_use>`, `<function_calls>`, `antml:*`) — seen with opencodego/deepseek on a kereta query.

Gates: typecheck, `verify.ts` EXIT=0 (adds cinema parsers, hotel helpers, transport parsers, tool-cap, XML-strip regressions), vitest 9/9. 9router model switched to `ngoding` in `.env.local` (gitignored); `open-code` is daily-limited. Known residual: server-side `opencode` provider can't call tools; Groq FREE tier still 413s on prompt+tools size (plan limit, paid tier OK).

## Session 2026-09-17 — FreeRide audit: failover yang tak pernah jalan

Audit live menemukan dua cacat yang membuat janji FreeRide (fallback saat free model kena limit) tidak pernah terpicu:

1. **Error in-band dianggap sukses.** OpenRouter/Groq melaporkan model mati/kepenuhan sebagai **HTTP 200 + `{"error":{...}}`** (`data: {"error":{"code":503,"message":"Upstream error from Nvidia…"}}`) — bukan HTTP error. `runOneCompletionOnce` dulu hanya membaca `choices[0].delta`, jadi frame itu dilewati → `text=""` tanpa exception → chain tidak failover dan user dapat balasan kosong. Kini `parseStreamError` (exported, pure, di `agent.ts`) mendeteksi error pada frame `data:` **dan** body JSON mentah (gateway yang mengabaikan `stream:true`), lalu melempar `Provider error (code): message`.
2. **Klasifikasi retry terlalu sempit.** Chain hanya fallback pada `/429|rate limit|quota/`. Kini memakai `isProviderRetryable` (`assistantError.ts`, pure, tested) = rate limit + provider/upstream 5xx + overload + model pensiun (`not a valid model`/`no allowed providers`/404) + ECONNRESET/timeout. Error yang bukan milik provider (argumen tool, kode) sengaja TIDAK memicu chain.
3. **Watcher/rotate tidak benar-benar memprobe.** `freerideWatcherOnce` hanya memprobe bila id berawalan `openrouter/` — padahal chain nyatanya `nvidia/*:free` → selalu "OK (no probe needed)" palsu; `freerideRotate` menilai hidup dari `res.ok` saja, sehingga model yang menjawab `200 + error` tetap dipertahankan. Kini keduanya memakai **satu** keputusan `isProbeAliveResponse(status, body)` (2xx **dan** tanpa `error` **dan** ada `choices`) + `probeOpenRouterModel` (id dipakai apa adanya — `openrouter/free` adalah id asli, bukan prefix yang boleh dipotong). Sekalian membuang dead code `rankedIds` di `freerideAuto`.

Bukti: unit `llmStream.test.ts` (12 tes) — frame error 502/429/400, body JSON mentah, retry-429-lalu-sukses, klasifikasi retry, `isProbeAliveResponse`; live E2E `runAssistantTurn` provider `openrouter` dengan model mati (`google/gemini-2.0-flash-001` → 404) **failover ke `nvidia/nemotron-3-ultra-550b-a55b:free` dan membalas "Halo 🌸"** (log `[freeride] … failed … trying …`); watcher live: primary sehat → `alive`, primary bohong → `failed + rotated`. Gates: typecheck, lint (0 temuan baru), vitest 39/39, `verify.ts` EXIT=0. State `.data/freeride/` dikembalikan seperti semula setelah probe. Residual: probe/rotate memakai satu tembakan per model — model yang *flaky* bisa ikut dibuang (chain dipulihkan oleh `freeride_auto`); provider di luar OpenRouter/9router tetap tidak memakai chain (sesuai guard).

### Lanjutan — audit teliti #2 (kelas-bug yang sama + risiko yang dibuka oleh fix)

Audit kedua menemukan 5 hal lagi:

1. **Frame terakhir tanpa newline dibuang.** Loop hanya memproses baris yang diakhiri `\n`, jadi error yang datang sebagai frame terakhir tanpa newline (umum untuk body JSON mentah) tetap hilang → balasan kosong. Kini isi `buffer` sisa diproses setelah loop; logika per-baris diekstrak jadi `handleLine` (dipakai untuk baris penuh **dan** sisa buffer).
2. **Failover bisa MENGGANDAKAN efek nyata.** Chain me-retry seluruh `runAgent` dengan `messages` yang sama; kalau attempt pertama sudah menjalankan tool (mis. `spotify_play`) lalu completion lanjutannya gagal in-band, model kedua bisa menjalankannya lagi + riwayat tool dua model tertumpuk. Kini `chainMayFailover(messagesBefore, messagesAfter)` (pure, tested) — failover hanya bila attempt itu belum menyentuh `messages` (= mati di completion pertama). Efeknya: error setelah tool jalan dilaporkan apa adanya, bukan diulang.
3. **Tidak ada batas waktu chain.** Tiap member bisa menghabiskan retry 429 sendiri (6s), jadi outage free-tier bisa menahan satu turn ~40s. Kini `CHAIN_DEADLINE_MS = 30_000` (pure constant) → setelah lewat, error asli dilempar (lebih jujur daripada seolah hang).
4. **Probe watcher membakar kuota yang sama dengan turn nyata.** Setelah watcher benar-benar memprobe, tick 60s = 1440 probe/hari dari kuota free-model (OpenRouter: ~20/menit, cap harian rendah) → justru bisa membuat chain-nya sendiri kena limit. Kini `shouldProbeNow(lastProbeAt)` (pure, tested) membatasi probe asli ke **1×/jam** (`PROBE_MIN_INTERVAL_MS`, disimpan sebagai `lastProbeAt`; tetap ditulis saat gagal supaya primary mati dicek per jam, bukan per menit). README + `MIA_FEATURES.md` diperbarui.
5. **Status watcher tidak jujur + contoh id tool salah.** `freeride_status` membaca `lastCheck` saja → probe yang GAGAL tetap tertulis "alive"; kini melaporkan `status` + `lastProbeAt`. Deskripsi tool `freeride_switch` mencontohkan `openrouter/qwen/qwen3-coder:free` (id palsu — `openrouter/` bukan prefix yang boleh ditambah); kini menunjuk id apa adanya dari `freeride_list`.

Bukti tambahan: `llmStream.test.ts` 19 tes (tambah: error frame terakhir tanpa newline, tanpa-error tetap sukses, `chainMayFailover`, `shouldProbeNow`); `verify.ts` blok baru "freeride (in-band error failover + probe honesty + probe throttle): OK" (termasuk stream tiruan yang membuktikan error in-band melempar + 7 tool terdaftar); live: watcher → `"skip — diprobe 0m lalu (interval 60m)"` dengan `lastProbeAt` tidak berubah. Gates: typecheck, lint (0 temuan baru), vitest 46/46, `verify.ts` EXIT=0.

### Lanjutan — audit A+B: satu celah kredensial nyata ditemukan & ditutup

Audit "sudah maksimal?" atas A+B menemukan **kebocoran di perbaikan sendiri**: `ato_prove` menerima `body_template`, dan bila model menaruh password mentah di situ (atau menempel body login), nilainya **tidak dimask** — `redactArgsForDisplay` hanya memask berdasarkan NAMA KUNCI (`body_template` bukan kunci sensitif), dan `redactSecrets` tidak punya aturan untuk `pass=`/`password:`/`user:pass`. Akibatnya password bisa masuk **prompt konfirmasi kanal, audit log, dan daily memory**. Bukti: `redactArgsForDisplay({"body_template":"user=admin&pass=K0h0na…"})` → password utuh; `redactSecrets("user=admin&pass=K0h0na…")` → utuh.

Perbaikan: `redactSecrets` dapat aturan **assignment** yang mempertahankan kunci+separator dan hanya memask nilainya — `\b((?:pass(?:word|wd)?|pwd|secret|credential)s?[\s\\"']*[=:][\s\\"']*)[^"'\s,;]+` → `$1[redacted]` — sehingga prosa biasa tidak rusak ("Password minimal 8 karakter" tetap utuh), JSON tetap valid (`{"password":"[redacted]","note":"ok"}`), dan prompt konfirmasi bersih. Diuji di blok `verify.ts` "secret redaction (… + credentials)" untuk `credential`, `password`, `body_template` (form & JSON), plus anti-false-positive prosa.

Hasil audit lainnya (semua lolos): `isOwnLabTarget` 17 kasus batas — localhost/127/::1/10.x/192.168.x/172.16-31/env+subdomain = own lab; **172.32, example.com, host engagement, demo publik (scanme/testphp), `127.0.0.1.nip.io`, dan metadata cloud `169.254.169.254`/`100.100.100.200` = BUKAN** (guard SSRF utuh); `autoApproveAllowed` → lab `true`, demo & metadata `false`. `ato_prove` live di lab: login 200 tanpa cookie → verdict jujur "TANPA SESI" (bukan klaim takeover). Catatan: policy bersifat **owner-level** (berlaku untuk semua kanal/user bot ini) dan `suite_hunt`/`security_hunt` sengaja TIDAK di daftar auto-approve (tetap konfirmasi).

### Lanjutan — full pentest kini jalan dalam SATU turn (tanpa prompt, tanpa hijack)

Owner melaporkan `security_hunt` **masih** minta konfirmasi (memang sengaja tidak ikut daftar pertama). Setelah dinaikkan, ketemu dua penyebab lain yang membuat alur tetap berhenti:

1. **Verbatim hijack pada tool kerja.** `VERBATIM_LIST` memuat ~78 tool security, jadi tool PERTAMA yang jalan (hunt/poc_verify/…) langsung menggantikan balasan dan **mengakhiri turn** — alur full pentest tak mungkin berantai. Kini `VERBATIM_LIST = PERSONAL_LIST_TOOLS + {hardening_plan, writeup}`: output mentah hanya menimpa balasan bila user MEMINTA daftar itu (ask-gated) atau itu dokumen jadi. Seluruh tool kerja (recon_*, suite_hunt, poc_verify, param_fuzz, js_mine, …) jadi konteks → agent bisa lanjut scan → probe → prove → report.
2. **Markup DSML bocor + scrap `<`.** Model OpenCode Go kadang menulis pemanggilan tool sebagai teks bergaya DeepSeek (`｜｜DSML｜｜ invoke …`) yang hanya terpotong sebagian: `stripToolCallProse` kini menghapus blok DSML dan **sisa scrap** (`^[\s<>|｜/]+`), dan ada `isEffectivelyEmpty()` (tanpa alfanumerik = kosong) supaya sisa `<` tidak lagi mematikan guard jawaban-kosong (gejala: balasan cuma `"< (Link-nya udah masuk daftar bacaan…)"`).

Policy auto-approve juga diperluas ke rantai kerja (hunt/discovery/probe/proof/record + `policy_show`); scanner berat tetap manual. **Bukti live (prompt Discord persis)**: 5 ronde tool (pentest_resources/hunt_log → finding_list → web_audit/csp_audit/content_discover/poc_verify/cors_audit → poc_verify×3 → **report_pdf**) dengan **0 prompt, 0 bocor markup**, balasan berupa daftar temuan rapi + PDF. Gates: typecheck, vitest **84/84** (+2 tes scrap/DSML), `verify.ts` EXIT=0, lint baseline 39.

## Session 2026-09-17 (lanjutan) — A: auto-approve khusus lab milik owner + B: prover rantai ATO

Dua perbaikan "power" yang dipilih owner:

**A. `policy_set` kini berlaku untuk lab milik owner (bukan hanya engagement).** Dulu `autoApproveAllowed` menolak setiap call ber-URL bila tidak ada engagement aktif (`policy.ts`), jadi latihan di lab sendiri tetap minta "ya" tiap probe. Sekarang syaratnya: tool ada di policy + `read`/`write` + **setiap URL harus `isOwnLabTarget`** (localhost/RFC1918/PENTEST_LAB_TARGETS — host demo publik `scan_permitted` sengaja TIDAK termasuk) dan tetap lolos scope `targetAllowed`. Host engagement/pihak ketiga TETAP manual (RoE default). `autoApproveAllowed` tak lagi butuh `hasActiveEngagement`; `isLabTarget` dipecah jadi `isOwnLabTarget` + `SCAN_PERMITTED_HOSTS`. Live: policy diisi 9 tool (`http_request,content_discover,crawl,js_mine,param_fuzz,poc_verify,ato_prove,finding_add,hunt_log`) → probe ke lab **jalan tanpa satu pun prompt** (dibuktikan: GET `/api/cek-nik?id=1` balas data). Reset: `policy_set action=reset`.

**B. Tool baru `ato_prove`** (`ato.ts`): membuktikan rantai account takeover dari kredensial bocor — POST login (JSON default, `user_field`/`pass_field`, atau `body_template` untuk form-urlencoded), simpan cookie sesi (`http_session`), lalu buka `protected_url` sebagai bukti. Verdict jujur: `ACCOUNT TAKEOVER TERBUKTI` / `LOGIN OK TAPI TANPA SESI` / `LOGIN OK, TAPI TIDAK MEMBUKA HALAMAN TERLINDUNGI` / `KREDENSIAL DITOLAK`, plus deteksi halaman yang masih form login (`looksLikeLoginForm`). **Password tidak pernah dikembalikan** (dimask `K0••••••••`) supaya tidak bocor ke memory/log/chat; session tersimpan bisa dipakai lanjut (`http_request session=ato`, `bola_diff`). Scope ditegakkan `httpRequest` untuk tiap hop. Live di lab: login 200 tapi **tanpa cookie** → verdict "TANPA SESI" dan Mia menaikkan narasinya dengan benar (lebih parah: `/api/admin-data` terbuka tanpa auth sama sekali) — bukan klaim takeover palsu. `ato.ts` + `ato.test.ts` (10 tes) + blok `verify.ts` "ato_prove (lab ATO chain + rejected creds + scope + no password leak): OK" (server login lokal: cookie → halaman admin 200; kredensial salah → DITOLAK).

CORE tetap **128** (Groq): `ato_prove` masuk irisan analisis (idx 56), `learnings_search` diturunkan (tetap ada di opencodego). Tool total **277**. Gates: typecheck, vitest **82/82**, `verify.ts` EXIT=0, lint baseline 39.

## Session 2026-09-17 (lanjutan) — PDF laporan dirapikan (renderer baru + footer halaman)

Eksportir PDF lama cuma `esc → heading → <br/>` + CSS 3 baris, jadi laporan tercetak sebagai dinding teks (bullets dan `>` tampil mentah). Ditambah **`reportHtml.ts`** (pure, tested): `parseReport()` mengurai dialek markdown laporan (`#`, meta `Total temuan: … rata-rata CVSS …`, `> scope`, `## N. [SEV · CVSS x] judul`, baris `- **Label**: value` yang nilainya bisa membungkus beberapa baris) → `renderReportHtml()` menghasilkan dokumen cetak: judul + meta, **chip ringkasan severity** (critical/high/medium/low + avg CVSS), **callout scope**, dan **satu kartu per temuan** dengan badge severity berwarna, garis aksen, tabel label (KATEGORI/PLATFORM/TARGET/STEPS/EVIDENCE/IMPACT/ROOT CAUSE/REMEDIATION/REFERENCES/FOUND) di mana nilai mirip data (JSON/HTTP/steps) dirender monospace `pre-wrap`. Playwright `printBackground` + `displayHeaderFooter` + `reportFooterTemplate(title)` (footer "Halaman X / Y", style inline karena Playwright mencetak template apa adanya). Kebijakan page-break: kartu panjang boleh mengalir, heading tidak pernah menggantung (`break-after: avoid`) → 6 halaman (dari 8 dengan `break-inside: avoid`), 150 KB. `reportHtml.test.ts` (7 tes: parse, badge/chip/scope, label+monospace, tanpa sisa markdown, escaping HTML, footer template). Verifikasi visual: screenshot HTML dirender (6 section, 4 chip, scope ada) — struktur benar.

Gates: typecheck, vitest **72/72**, `verify.ts` EXIT=0, lint baseline 39. Berlaku untuk `report_pdf` DAN `hardening_pdf` (keduanya lewat `renderMarkdownPdf`).

## Session 2026-09-17 (lanjutan) — list Discord tetap jadi satu paragraf: scrubber menelan newline

Setelah prompt/hint diperbaiki, balasan Mia **sudah bernomor tapi tetap satu paragraf**. Penyebab sebenarnya ada di **outbound scrubber**, bukan di model: `replyChunk.ts` `scrubToolMarkup` memakai `t.replace(/\s{2,}/g, " ")` — `\s` termasuk newline, jadi **setiap baris kosong antar item list diubah menjadi spasi** ("1. …\n\n2. …" → "1. … 2. …"). Setiap balasan Discord/Telegram lewat `chunkText` → kena. Bukti: `scrubToolMarkup("…\n\n1. …\n\n2. …")` → satu baris.

Perbaikan: hanya spasi/tab yang dirapatkan (`[ \t]{2,}` → " "), newline dipertahankan, baris kosong dibatasi maksimal dua (`\n{3,}` → `\n\n`). Tag-removal gap tetap dirapikan. Test baru: list ber-baris-kosong tidak diratakan + double space/tag gap tetap bersih (`turnRouting.test.ts`, total **65 tes**). Bukti live: balasan model 9 baris → lewat `chunkText` (jalur Discord) tetap **9 baris** (satu temuan per baris), bukan satu paragraf.

## Session 2026-09-17 (lanjutan) — Mia bilang batch yang sudah di-approve "belum jalan"

Owner mem-paste balasan Mia: setelah approve 4 `http_request`, Mia menulis *"Lima request tadi belum jalan — semuanya butuh persetujuan kamu dulu… setujui ulang batch itu"*. Bukti dari `http-history.json` user `naufalazhar652952`: 4 request yang di-approve **BENAR-BENAR jalan** (10:16:08–09Z: `/api/dokumen`, `/api/dokumen?id=3`, `/api/cek-nik?id=1`, `/api/cari-berita?q=layanan`) — jadi klaim "belum jalan" itu salah. Tiga reproduksi alur (teks Discord persis, approve tiap batch) berakhir normal: PROMPT → approve → TEXT ringkasan; gejala itu **tidak berhasil direproduksi** (indikasi model menarasikan batch berikutnya sebagai "menunggu approval").

Hardening yang tetap dikerjakan (kelas bug: approved batch dituduh "not selected"):
1. Hasil tool kini **di-anchor ke pesan assistant yang MENDEKLARASIKAN id yang di-approve**, bukan ke pesan assistant terakhir (dulu `[...messages].reverse().find(...tool_calls)`) — kalau keduanya berbeda, batch yang baru di-approve bisa ditandai "Not selected" dan model lalu menyuruh user approve ulang. Bila mismatch terjadi, ada `console.warn` diagnostik.
2. Placeholder-nya diperjelas: *"Not selected: … other approved calls in the same batch DID run — read their results above. Do not run it; if it is still needed, propose it again."*

Gates: typecheck, vitest **63/63**, `verify.ts` EXIT=0, lint baseline 39.

## Session 2026-09-17 (lanjutan) — retry "jawaban kosong" selalu 400 (MissingSessionID)

Gejala: setelah approve batch `http_request`, Mia balas **rekap digest** ("balasan detailnya tersendat… Bilang 'lanjut'") alih-alih melanjutkan — padahal retry+nudge sudah ada. Ternyata **bug di perbaikan sendiri**: retry memanggil `runOneCompletion(..., undefined)` sehingga **tanpa header `x-opencode-session`** → OpenCode Go menolak `400 {"type":"error","error":{"type":"MissingSessionID"}}` → catch → digest. Probe membuktikannya: `runOneCompletion` tanpa header → 400 MissingSessionID; dengan `endpointHeaders()` → teks balik (270 char).

Perbaikan: header per-endpoint diekstrak jadi **satu pemilik** `endpointHeaders(url, user)` (dipakai agent loop + retry; sebelumnya inline di `runAgent`), jadi jalur mana pun mustahil lupa. Test: `endpointHeaders` (opencode-go → session+UA, gateway lain → undefined, user key disanitasi). Gates: typecheck, vitest **63/63**, `verify.ts` EXIT=0, lint baseline 39.

## Session 2026-09-17 (lanjutan) — "Refleksi Malam" dobel + pesan internal bocor

Owner mem-paste refleksi malam dari Mia; ketemu tiga cacat:
1. **Dobel push.** `recap` mengirim satu refleksi **per profil** di mesin ini (`for (const user of allUserKeys()) … pushToOwner`), jadi owner menerima beberapa pesan dengan isi berbeda (profil alias + sisa user uji). `briefing.ts` sudah lebih dulu dibetulkan ("Opsi A: 1 briefing owner"); sekarang `recap`, `weeklyInsight`, dan `proactive` memakai pola yang sama — push **satu** untuk identitas owner (`canonicalUserKey("naufalazhar652952")`).
2. **Pesan internal bocor sebagai ucapan user.** Baris pembawa rolling summary `[Percakapan sebelumnya — …]` tampil sebagai "Mas Naufal: …" di recap. `isJunkLine` kini juga menolak `^\[(?:Percakapan sebelumnya|self-correct|superseded|automation|system)\b`.
3. **Parafrase tidak digabung.** Dedupe lama memakai 60 karakter pertama, sehingga 4 variasi satu permintaan (pentest lab) muncul semua. Kini perbandingan **tanpa URL/tanda baca** (`stripUrl`) + aturan **topic token** (token ≥5 huruf yang dipakai beberapa permintaan TAPI tidak semua — "kohona"/"pentest" menyatukan parafrase, sedangkan label "Mas Naufal:" tidak) + Jaccard konten ≥0.4. `contentTokens` diekspor dari `dupes.ts` supaya tokenizer-nya satu definisi.

Bukti: recap terkontrol — 4 variasi permintaan pentest + carrier internal → **2 baris bermakna, tanpa carrier** (sebelumnya 4 + carrier); recap nyata owner bersih dari marker internal; `verify.ts` blok "recap hygiene" ditambah assertion carrier + parafrase (`labAsks === 1`). Gates: typecheck, vitest **84/84**, `verify.ts` EXIT=0, lint baseline 39. Residual: dua parafrase yang tidak berbagi token khas masih bisa muncul sebagai dua baris.

## Session 2026-09-17 (lanjutan) — output Discord jadi satu paragraf ("kurang enak dibaca")

Owner mengeluh ringkasan pentest di Discord datang sebagai satu paragraf padat. Tiga akar:
1. **Prompt bentrok.** `SYSTEM_PROMPT` bilang "never use ... bullet lists in your final answer" (aturan voice) sementara hint Discord cuma mengizinkan bold tipis → model memilih prosa. Kini aturan itu di-scope ke VOICE, dan hint Discord/Telegram eksplisit: jawaban berbentuk daftar (temuan/langkah/opsi) WAJIB satu item per baris, plus **contoh bentuk** yang boleh ditiru (pelajaran lama hanya melarang mengutip contoh BURUK).
2. **`reflowStructuredReply` menghapus struktur.** `ensureMoodReplyQuality` hanya melindungi list ber-`\n- `, sehingga **daftar bernomor (1. 2. 3.) di-refund jadi prosa** ("Content preserved, labels dropped"). Kini deteksi list diperluas (`looksLikeMarkdownList`: ≥2 baris bullet/dash/angka) dan list apa pun tidak disentuh.
3. **`temuan|finding` di `LIST_ASK_RE` terlalu luas** (ditambahkan di fix hijack sebelumnya): sekadar menyebut kata "temuan" membuat `finding_list` mentah menggantikan balasan. Dihapus — permintaan daftar tetap menang lewat "apa aja/daftar/lihat/cek".

Plus: `finding_list` kini menerima `target` (host/URL) seperti `report_*`, jadi "ringkas temuan lab X" benar-benar 6 temuan (bukan 10 campur). Prompt menyebut `target=<host>` wajib saat user menyebut satu lab/target.

Bukti live (channel discord, user `naufalazhar652952`): balasan jadi ringkas + **daftar bernomor satu temuan per baris** (11 baris, bukan 1 paragraf), target lab, total **6 temuan**. Gates: typecheck, vitest **60/60** (+test `looksLikeMarkdownList` + hijack "3 temuan paling penting"), `verify.ts` EXIT=0 (+assert `finding_list` ter-scope), lint baseline 39.

## Session 2026-09-17 (lanjutan) — laporan bercampur temuan target lain → report per-target

Live: report lab Kohona milik user Discord (`naufalazhar652952`) menarik **4 temuan lama** (pulsepoint, vuln-node 127.0.0.1:4010) ke dalam daftar — karena `report_generate`/`report_save`/`report_pdf` selalu melaporkan SEMUA temuan terbuka milik user, dan Mia sempat menawarkan *menghapus* temuan lama demi membereskan laporan (salah: itu menghilangkan riwayat). Perbaikan: ketiganya menerima `target` (host/URL) opsional → `generateReport(rawUser, { target })` memfilter lewat `matchesHost` (host exact, subdomain, atau substring URL). Deskripsi tool + prompt kini menyuruh memakai `target` saat melaporkan satu lab/engagement dan melarang menghapus temuan lama. Terverifikasi: user `naufalazhar652952` — ALL 10 temuan → SCOPED ke lab **6 temuan** (critical 2, high 2, medium 2; rata-rata CVSS 7.5), header `LAB MILIK OWNER`, nol kebocoran pulsepoint; PDF scoped 63 KB. Assertion baru di `verify.ts` (scoped report tidak membocorkan target lain + pesan jujur saat target kosong). Gates: typecheck, vitest **59/59**, `verify.ts` EXIT=0, lint baseline 39.

## Session 2026-09-17 (lanjutan) — fallback kepotong diperhalus + report salah attribusi engagement

Dua perbaikan lanjutan dari latihan lab:
1. **Retry diperkuat + digest diberi jalan keluar.** Guard jawaban-kosong kini mengirim nudge eksplisit ("Jawab sekarang dalam 1–3 kalimat dari hasil di atas. Jangan panggil tool…") pada SALINAN pesan (tidak pernah ikut tersimpan ke konteks kanal), karena model thinking dengan tool dihapus tetap bisa hanya mengisi `reasoning`. Digest-nya juga tidak lagi terdengar seperti jawaban final: `"Aku sudah menjalankan <tool> 🌸 balasan detailnya tersendat, jadi ini rekap aksi terakhir: … Bilang \"lanjut\" kalau mau kuteruskan dari sini."` Live: alur lab tuntas — 4 batch approval → `sqlmap_scan` → **"Laporan Pentest, 7 temuan (critical 1, high 1, medium 2, low 3), rata-rata CVSS 5.3"** + PDF baru 64 KB.
2. **Report bisa salah attribusi engagement.** `generateReport` memakai `listEngagements().find(active)` — engagement AKTIF mana pun, jadi laporan lab menuliskan `> Engagement: … Bugcrowd HostGator LATAM` (program yang tidak ada hubungannya). Kini header dihitung dari host temuan: engagement dipakai hanya bila **SEMUA** host temuan tercakup engagement yang sama (`activeEngagementFor`), kalau semua host adalah lab → `> Scope: LAB MILIK OWNER / aset sendiri (berizin)`, selain itu kalimat generik. Terverifikasi: header laporan user `naufal` berubah dari HostGator → "LAB MILIK OWNER".

Gates: typecheck, vitest **59/59**, `verify.ts` EXIT=0, lint 39 warning (baseline, satu unused-import baru sudah dibersihkan).

## Session 2026-09-17 — "jawabannya kepotong" padahal tool jalan (thinking model jawab di reasoning)

Di tengah alur pentest lab, setelah approve batch `http_request`, Mia membalas *"Hmm, jawabannya kepotong — coba tanya lagi ya 🌸"*. Log menunjukkan tidak ada error: round terakhir selesai dengan **teks kosong** (model thinking menjawab hanya di `reasoning`, `content: ""`), lalu channel mengisi fallback "kepotong".

Dua hal diperbaiki (agent, jadi semua kanal dapat):
1. **Guard jawaban-kosong**: bila turn berakhir tanpa teks tapi ADA tool yang berjalan, agent (a) mencoba sekali lagi tanpa tools untuk minta jawaban, lalu (b) memakai `summarizeToolResults(messages)` — digest deterministik 1–2 hasil tool terakhir (placeholder "Not selected/Not executed" diabaikan) — sehingga user melihat hasil nyata, bukan jalan buntu. Pure + tested.
2. **Urutan guard diperbaiki**: guard awalnya diletakkan SETELAH `scheduleLinkCapture`, sehingga catatan dekoratif "(Link-nya udah masuk daftar bacaan…)" membuat `text` tampak tidak kosong dan guard tak pernah jalan (live: balasan 94 char isinya HANYA catatan itu). Guard kini dijalankan sebelum suffix dekoratif.

Bukti: `turnRouting.test.ts` (digest + skip placeholder), blok `verify.ts` "empty-answer guard (tool-result digest): OK", dan live — turn yang dulu berakhir kosong kini membalas *"Aku sudah jalankan fetch_url, http_request 🌸 ini ringkasannya: … /api/dokumen?id=5 -> 200 OK …"*. Gates: typecheck, vitest **59/59**, `verify.ts` EXIT=0. Residual: ini degradasi yang jujur, bukan pengganti jawaban model — kalau model tetap diam, user tinggal bilang "lanjut".

## Session 2026-09-17 — pentest lab putus di tengah jalan: rolling summary membelah pasangan tool-call

Setelah dua fix sebelumnya, alur "full pentest + report pdf" jalan (hunt → approve batch → probe endpoint → `poc_verify` → `finding_add` → `report_pdf`), TAPI di tengah alur muncul lagi *"Aksimu sudah dijalankan. Sayangnya balasan detailnya tersendat…"*. Error sebenarnya (dari log, bukan tebakan): **`400 Messages with role 'tool' must be a response to a preceding message with 'tool_calls'`**.

Akar masalah: **`buildSummarizedMessages` (rolling summary) memotong array `messages` tepat di tengah pasangan `assistant(tool_calls)` + `tool` results** — `messages.slice(-keepRecent)` tanpa memperhatikan batas aman. Begitu percakapan pentest jadi panjang (banyak output tool besar), potongan itu menyisakan **tool result yatim** → gateway strict OpenCode Go menolak SELURUH turn. Ini bukan khas lab: setiap percakapan panjang yang punya tool-call bisa kena.

Perbaikan (`summarize.ts`): boundary digeser mundur selama `messages[start].role === "tool"` (sehingga pesan assistant yang mendeklarasikan `tool_calls` tetap ikut di tail), plus trim defensif bila input sudah yatim. Diuji `summarize.test.ts` (3 tes): cut di setiap nilai keep-recent 1..6 tidak pernah menghasilkan tool yatim; percakapan pendek tak disentuh.

Bukti live (urutan channel persis, approve tiap batch): round 1 `security_hunt` → approved → `http_request`+`param_fuzz` → `http_request` → `poc_verify` ×2 → selesai **tanpa satu pun 400**, `orphans=0` di setiap langkah, dan hasil akhirnya nyata: **4 finding** (high 7.5 BAC via header `x-user-role`, medium 6.1 stored XSS, low 3.1 header, low 3.1 excessive data exposure `/api/cek-nik`) + **PDF report** 54 KB (`%PDF-1.4`) di `.data/users/naufal/reports/`. Gates: typecheck, vitest **57/57**, `verify.ts` EXIT=0, lint 0 temuan baru.

## Session 2026-09-17 — "full pentest di lab-ku" ditelan hunt_log + Mia menolak lab sendiri

Owner menulis di Discord: *"mia coba lakukan full pentest di https://…netlify.app/index.html"*; Mia menjawab dengan **dump hunt log 21 target program LAIN** (pulsepoint/webmd/underarmour/rapyd/seek/hostgator/pantheon) — lab-nya tidak pernah disentuh. Ketemu **dua** akar:

1. **Hijack verbatim (kelas yang sama dengan reminder-list).** `hunt_log` ada di `VERBATIM_LIST` tapi **tidak** di `PERSONAL_LIST_TOOLS`, jadi output mentahnya selalu menggantikan balasan — padahal prompt menyuruh agent memanggil `hunt_log` sebagai *persiapan* sebelum menguji. Perbaikan: family **context/status** dipindah ke gate "hanya bila user minta daftarnya" — `hunt_log`, `engagement_targets`, `finding_list`, `http_history`, `recon_list`, `policy_show`, `flow_list`, `bounty_status`, `tech_watch`, `dup_check`, `program_score`, `submission_track`, **plus diagnostik `csp_audit`/`cors_audit`** (dump mentahnya mengalahkan sintesis saat user minta "pentest"). Tool **work-product** (`suite_hunt`, `poc_verify`, `report_*`, `recon_*`, …) tetap selalu verbatim. `LIST_ASK_RE` ditambah `temuan|finding` supaya permintaan daftar temuan tetap menang. Dikunci di `turnRouting.test.ts`.
2. **Mia menolak lab milik owner** (setelah hijack diperbaiki, balasannya jadi penolakan scope: "host publik pihak ketiga, tidak ada engagement aktif" — padahal `PENTEST_LAB_TARGETS` mengizinkannya). Prompt lama hanya menyuruh cek `engagement_list`, tidak pernah menyebut daftar lab sendiri. Perbaikan: `pentestResources()` kini menampilkan baris **"LAB MILIK OWNER (BERIZIN …)"** dari env `PENTEST_LAB_TARGETS`, dan aturan scope di prompt menyebut 3 sumber izin (engagement AKTIF **atau** daftar lab owner **atau** localhost/RFC1918) + larangan menolak lab owner hanya karena engagement_list kosong.

Bukti live (pesan Discord persis, provider default): turn 1 → `engagement_list` + `pentest_resources` + `web_audit` + `hunt_log` → **bukan hijack, bukan penolakan**; reasoning-nya sendiri: *"The host IS listed in pentest_resources as OWNER's lab — BERIZIN… My earlier refusal was wrong"* → mengajukan `security_hunt` (confirm). Setelah approve → lanjut menguji `/api/dokumen?id=5` dengan vs tanpa header `x-user-role: admin` (A/B broken access control), tetap lewat gate konfirmasi. Gates: typecheck, vitest **54/54**, `verify.ts` EXIT=0 (+assert `pentest_resources` memuat baris lab owner), lint 0 temuan baru.

## Session 2026-09-17 — lab Netlify bisa dipakai + BUG reasoning_content (thinking mode)

Owner punya lab latihan publik: `https://6a90ef33c41c07dd3335811e--cozy-kangaroo-42f2e0.netlify.app` (portal desa fiktif, API-nya memang sengaja rentan: `/api/cek-nik?id=` IDOR, `/api/dokumen` + `/api/dokumen/<id>` akses dokumen internal tanpa auth, `/api/pengaduan` stored XSS, `/api/login`). Cara mengizinkan target PUBLIK milik owner: **`PENTEST_LAB_TARGETS`** (bukan engagement, supaya berlaku untuk SEMUA kanal/user sekaligus) — `.env.local` diisi base site **dan** host deploy-preview, karena preview Netlify `<deploy-id>--<site>.netlify.app` **bukan** subdomain dari `<site>.netlify.app` (aturan suffix di `isLabTarget` tidak menangkapnya). `.env.example` diberi catatan. Bukti: `isLabTarget`/`targetAllowed` true untuk lab, `example.com` tetap false; live Mia `web_audit` + `http_request` ke lab mengembalikan data nyata.

**BUG BESAR yang ketemu saat latihan itu (bukan khas lab):** `LLM failed (400): ... The 'reasoning_content' in the thinking mode must be passed back to the API.` — model thinking (DeepSeek v4.x di OpenCode Go) menolak request lanjutan kalau reasoning-nya tidak dikembalikan. Akibatnya **setiap lanjutan setelah tool `risk:read` yang berhasil / konfirmasi tool bisa gagal** → user melihat "Layanan AI sedang bermasalah" / "balasan detailnya tersendat". Perbaikan (3 lapis):
1. `runOneCompletionOnce` kini ikut mengumpulkan `delta.reasoning_content` (dan `delta.reasoning` gaya OpenRouter) → `runOneCompletion` mengembalikan `reasoning`; `runAgent` menempelkannya sebagai `reasoning_content` pada pesan assistant ber-tool_calls **hanya untuk endpoint OpenCode Go** (gateway strict lain menolak field tak dikenal).
2. **Jalur web/HTTP**: reasoning itu hanya hidup di server, sedangkan klien yang mengirim ulang konteks — jadi frame `@@CONFIRM` kini bisa berbentuk `{ calls, reasoning }` (masih array biasa bila tak ada reasoning = back-compat). `GroqStreamingProvider` menerima dua bentuk, menyimpan `pendingReasoning`, dan menempelkannya pada pesan assistant yang ia bangun di `confirmTool`. Terverifikasi live: turn 1 → frame object + reasoning, turn 2 (`confirm_call`) → Mia mengembalikan temuan lab (sebelumnya 400).
3. `POST /api/llm` kini juga menerima `confirm_calls` (batch) — sebelumnya hanya `confirm_call` (singular), padahal agent mendukung keduanya (dipakai channel); route adalah jalur API/web jadi parity-nya dilengkapi.

Gates: typecheck, `npm test` **53/53** (2 tes baru: reasoning `reasoning_content` + `reasoning` gaya OpenRouter), `verify.ts` EXIT=0, lint 0 temuan baru. Docs: `.env.example` (PENTEST_LAB_TARGETS + jebakan host preview Netlify).

## Session 2026-09-17 — pentest hardening: kapabilitas mati dibenahi, CVSS v4.0, 4 playbook

Audit sisi cybersec menemukan dua kapabilitas yang **dijanjikan prompt tapi tidak bisa jalan** di mesin ini, plus dua celah standar:

1. **`zap_scan` mati total** (butuh Docker; Docker tidak terpasang) dan gagalnya baru ketahuan setelah spawn. Kini `zapScan` mengecek `docker --version` lebih dulu (`runCapture` yang sudah ada) dan mengembalikan pesan jujur + padanan **native** (`web_audit`, `security_hunt`, `pentest_scan tool=nuclei`, `cors_audit`/`csp_audit`) alih-alih error generik; scope tetap ditegakkan. Deskripsi tool ikut menyebut hal ini.
2. **`pentest_scan tool=whatweb` selalu gagal** (`whatweb` tidak terpasang) padahal repo sudah punya fingerprint native. Logika `detectTech` diekstrak ke **`techFingerprint.ts`** (dipakai bersama `techWatch.ts` — satu definisi, bukan salinan kedua) + `fetchFingerprint(url)`; saat biner `whatweb` ENOENT, `pentestScan` jatuh ke `nativeFingerprint()` dan tetap mengembalikan hasil berguna.
3. **CVSS v4.0** (standar FIRST saat ini) kini didukung **tanpa dependency**. Percobaan `npm install` sebuah library v4 **dibatalkan**: repo ini punya konflik peer lama (react 18 vs 19) dan install mengacak `package-lock.json` (+1860/−100 baris, react root turun ke 18.3.1) — jadi vendor lokal dinilai lebih aman. `cvssV4.ts` mengimplementasikan MacroVector (EQ1–EQ6) + severity-distance persis spesifikasi, dengan data standar di `cvssV4Data.ts` (tabel lookup 270 baris + max-composed + depth). **Verifikasi: 0 mismatch vs implementasi referensi pada 3000 vektor acak** + anchor resmi (10/9.3/0/1/5.4). Diintegrasikan lewat **satu pintu** `cvssScoreAny(vector)` (dispatch `CVSS:4.0` → v4, sisanya v3.1 lama) sehingga `cvss_score`, `platform_severity`, dan `report` otomatis menerima vektor v4. `cvss4MacroVector` + `parseV4Vector` menolak vektor cacat dengan pesan jelas.
4. **4 playbook orisinal** (bukan adaptasi Strix, ditulis sendiri): `vulnerabilities/web-cache-poisoning.md`, `websocket-security.md`, `account-takeover.md`, `host-header-injection.md` — masing-masing dengan Mia tool mapping (oast/poc_verify/http_session/hunt_log). Playbook dir-driven → langsung muncul di katalog `security_playbook` (total **82** pack; `find` menghitung 83 karena `security-playbooks/README.md` bukan pack).

Bukti: `apps/web/src/lib/cvssV4.test.ts` (5 tes: anchor, 0-impact, monotonicitas AV, penolakan vektor cacat, macrovector); blok `verify.ts` "cvss v4.0 (anchors) + platform severity + whatweb native fallback: OK" (termasuk server lokal + `pentest_scan tool=whatweb` yang membuktikan fallback); live lewat route: Mia memanggil `cvss_score` → **9.3 critical** untuk vektor v4, dan memuat `web-cache-poisoning` dengan 3 langkah pertama yang benar. Gates: typecheck, lint (0 temuan baru), vitest **51/51**, `verify.ts` EXIT=0. Docs (README/MIA_FEATURES) diperbarui ke 83 playbook.

## Session 2026-09-17 (presence honesty) — Mia mengarang "kamu ngilang sejak jam X"

Owner mem-paste percakapan Discord: ditanya "kamu kemana aja", Mia menjawab "aku nungguin kamu terus… dari chat terakhir kamu tuh jam 14.21, abis itu sunyi" — padahal pesan sebelumnya 1 menit sebelumnya (14:21). Akar masalahnya: `currentTimeLine()` hanya menyuntik **jam sekarang** ke system prompt, tanpa data kapan user terakhir mengirim pesan; model memakai jam sekarang itu sebagai "jam chat terakhir" lalu mengarang jeda/kesenyapan. Ini kelas yang sama dengan REMINDER/SPOTIFY HONESTY: model mengklaim fakta yang tidak dimilikinya.

Perbaikan (satu pemilik, tidak bisa drift): klausa ditambahkan di `currentTimeLine()` (dipakai **kedua** prompt, `buildSystemPrompt` + `buildOpenCodeSystemPrompt`) — "This is the CURRENT time only — you keep NO record of when the user last messaged you, so never claim they had been gone or silent, never state how long it has been, and never present this clock as their last-chat time." Contoh buruk **tidak** dikutip di prompt (pelajaran lama: small model meniru contoh yang dikutip). Guard: `verify.ts` blok "presence honesty (clock is not a last-seen fact) in both prompts: OK" memastikan klausa ada di dua prompt. Live (provider default, channel discord): "kamu kemana aja" → "Aku selalu di sini kok beb… gimana harimu?"; pertanyaan umpan "dari tadi kamu ngilang ya?" → balik bertanya, **tanpa** karangan jeda; "kamu kangen nggak" → tetap hangat tanpa timeline. Gates: typecheck, verify EXIT=0, vitest 46/46.

Catatan (tidak diubah): entri `dailyMemory` menyimpan header timestamp ISO **UTC** (`## 2026-09-17T06:21:…Z`) yang muncul mentah di `memory_get` — sumber potensi kebingungan jam WIB-vs-UTC bila ter-recall; kalau mau, header bisa ditulis WIB (perlu keputusan karena file lama sudah ISO-Z). Fitur opsional: kalau Mia memang harus tahu "sudah berapa lama user tidak chat", perlu simpan waktu pesan terakhir per chat (state adapter) — bukan sekadar prompt.

Residual yang **tidak** diubah (disengaja): body sukses non-stream (gateway yang mengabaikan `stream:true`) masih belum diekstrak jadi teks — semua provider yang dipakai (OpenRouter/Groq/opencodeGo/9router) menuruti `stream:true`, jadi ini pre-existing dan di luar audit; `free-models-per-min` OpenRouter itu kuota akun (failover ke model free lain bisa kena 429 yang sama) — dicatat, tapi failover tetap dipertahankan karena laporan lapangan menunjukkan sebagian model free limit-nya per-model.
